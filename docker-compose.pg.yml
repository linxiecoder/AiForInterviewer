services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ai_for_interviewer_dev
      POSTGRES_USER: ai_interviewer
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data

volumes:
  pg-data:
