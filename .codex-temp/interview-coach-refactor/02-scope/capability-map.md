---
title: Round 3 能力映射
type: temporary-capability-map
status: scope-locked
---

# Round 3 能力映射

## 1. 来源锁定

- interview-coach-skill source: `/tmp/interview-coach-skill`
- interview-coach-skill commit: `634a8dd8689e0420c21e5f0c8ae3cfa9e1a7ab7e`
- AIForInterviewer branch: `feature/interview-coach-refactor`
- AIForInterviewer commit: `cc94db2d79365b021e33096a4988c4864fd743d8`
- 本文件只记录临时能力映射，不是 active docs、ADR、BACKLOG、DELIVERY_PLAN、Goal 文件或实现事实。
- Adopt / Adapt 必须同时具备 interview-coach evidence 与 AIForInterviewer landing point；缺少明确落点的能力只能标记为 Defer / Reject / Research-only。

## 2. 决策词表

| 决策 | 含义 |
| --- | --- |
| Adopt | 可直接使用现有 AIForInterviewer 落点承接，后续只需按仓库流程创建 Goal / design / tests。 |
| Adapt | 可吸收能力模式，但必须改写为 AIForInterviewer 的现有数据、API、prompt、验证或 UI 边界。 |
| Defer | 有潜在价值，但当前代码或 active docs 缺少足够落点，暂缓进入实现范围。 |
| Reject | 与当前产品、治理或安全边界冲突，不进入后续范围。 |
| Research-only | 仅作为思考材料，不转成需求、设计或实现。 |

## 3. Capability Mapping 表

| ID | 能力 | interview-coach evidence | AIForInterviewer landing point | 决策 | 原因 | 关联 Goal | 验收标准 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R3-CAP-001 | source-backed command routing pattern | `/tmp/interview-coach-skill/SKILL.md` 的 `## Priority Hierarchy`、`## Command Registry`、`### File Routing`；`/tmp/interview-coach-skill/references/mode-detection.md` 的 `# Mode Detection Priority`、`## Multi-Step Intent Detection`；`/tmp/interview-coach-skill/references/commands/analyze.md` 的 `# analyze — Transcript Analysis Workflow`。 | 当前产品不是 agent command bundle。可观察落点只有 `apps/api/app/api/v1/polish.py::create_polish_question_task`、`create_polish_answer`、`create_polish_feedback_task`、`refresh_polish_progress_tree_state`，以及前端 `apps/web/src/entities/polish/api/polishApi.ts::POLISH_API_PATHS` 和 `apps/web/src/pages/interview/InterviewPage.tsx::InterviewWorkbenchPage`。当前行为是 HTTP/API/workbench action，不是 slash-command router；差距是不能复用 command names、menu 或 file-routing 结构。 | Research-only | 该能力是 agent skill activation / routing 形态，不是 AIForInterviewer 产品架构。只保留“入口轻、细节下沉到具体 contract/service”的架构启发。 | 不创建 Goal；Round 4 仅可作为设计背景引用。 | 后续文档不得复制 interview-coach command list、command names、directory layout、workflow wording 或 output wording；若引用，只能引用本 capability map 的 Research-only 结论。 |
| R3-CAP-002 | persistent coaching state pattern | `/tmp/interview-coach-skill/references/coaching-state-schema.md` 的 `# coaching_state.md Schema`、`## Score History`、`## Interview Intelligence`、`## Drill Progression`、`## Active Coaching Strategy`、`## Calibration State`；`/tmp/interview-coach-skill/references/schema-migration.md`；`/tmp/interview-coach-skill/references/state-update-triggers.md`。 | `apps/api/app/infrastructure/db/models/interview.py::InterviewSession` 记录 `binding_id`、`resume_id`、`job_id`、`mode`；`apps/api/app/infrastructure/db/models/interview.py::PolishSessionDetail` 记录 `progress_tree_status`、`progress_percent`、`progress_tree_plan_json`、`progress_tree_state_json`；`apps/api/app/application/polish/use_cases.py::_build_session_detail` 组装 `turns`、`progress_context`、`progress_tree_plan`、`progress_tree_state`；`apps/api/app/infrastructure/db/repositories/polish.py::update_progress_tree` 持久化 progress tree。当前行为是 DB-backed Polish session/progress state；差距是没有 flat-file `coaching_state.md`、没有 schema migration for coaching sections、没有 outcome/calibration/storybank sections。 | Adapt | 可以吸收“长期 coaching state 要结构化、可迁移、可压缩”的模式，但必须落到现有 SQLAlchemy model、repository 和 session detail，而不是复制 flat Markdown state。 | Round 4 候选 Goal 包：`R4-GP-state-continuity`，本轮不创建文件。 | 任何后续设计必须列出新增/复用字段、repository 方法、API response、frontend type、迁移/兼容策略和 tests；不得把 `coaching_state.md` 原样搬入仓库。 |
| R3-CAP-003 | capture-versus-analysis separation pattern | `/tmp/interview-coach-skill/references/commands/feedback.md` 的 `# feedback — Capture Feedback, Outcomes, and Corrections`、`### Input Type Detection`、`### Type A: Recruiter/Interviewer Feedback`、`### Type B: Outcome Report`、`### Design Principles`；`/tmp/interview-coach-skill/references/commands/progress.md`；`/tmp/interview-coach-skill/references/calibration-engine.md`。 | 当前已存在 answer capture 与 feedback analysis 分离：前端 `apps/web/src/pages/interview/InterviewPage.tsx::sendAnswer` 先调用 `createPolishAnswer`，再调用 `createPolishFeedbackTask`，反馈失败时显示“回答已保存”；API `apps/api/app/api/v1/polish.py::create_polish_answer` 与 `create_polish_feedback_task` 分离；repository `apps/api/app/infrastructure/db/repositories/polish.py::add_answer`、`add_feedback` 分离；测试落点包括 `tests/api/test_polish_application_service_split.py`、`tests/api/test_polish_feedback_generation_service.py`。当前行为只覆盖用户回答保存后再分析；差距是没有 recruiter/interviewer external feedback、outcome report 或 correction capture 对象。 | Adapt | “先保存事实输入，再异步/后续分析”与现有 answer/feedback 链路一致，可作为后续真实反馈输入设计原则；但 interview-coach 的 external feedback taxonomy 不能直接落实现。 | `G-002-capture-analysis-separation.md` Draft；Requirement: `R-003`、`R-004`；Functional Spec: `FS-003`、`FS-004`。 | 后续设计必须证明 answer capture 可独立成功、feedback analysis 只能消费已保存 answer、analysis failure 不撤销 answer 且不生成正式 Weakness/Asset/Training/Report/outcome calibration；当前 Polish answer-save 路径保持不变。 |
| R3-CAP-004 | transcript ingestion pipeline pattern | `/tmp/interview-coach-skill/references/transcript-formats.md` 的 transcript format detection / normalization；`/tmp/interview-coach-skill/references/transcript-processing.md` 的 `## Step 0.5: Format Detection and Normalization`、`## Step 1.5: Transcript Quality Gate`、`## Step 2: Format-Aware Parsing`、`## Step 3: Multi-Lens Scoring`；`/tmp/interview-coach-skill/references/commands/analyze.md`。 | Round 2 搜索未找到完整 transcript 一级对象。当前落点只是 Polish `Question` / `Answer` / `Feedback` records、`apps/api/app/application/polish/use_cases.py::_build_session_detail` 组装 turns、前端 `PolishSessionDetail.turns`。WebSocket/SSE 和 transcript upload 也未发现实现。当前行为是结构化 Q/A/feedback 工作台，不是 transcript parser；差距是缺 transcript source model、quality signals、normalization pipeline、API、UI 和 tests。 | Defer | 能力价值明确，但缺少 AIForInterviewer 当前实现落点；若强行 Adapt 会把未来 transcript capability 写成当前事实。 | 无；Round 4 不纳入默认 Goal 包，除非用户另行授权 transcript discovery。 | 不得在 Round 4 默认设计中加入 transcript ingestion。若后续开启，先做 read-only landing audit 和 threat model，再建独立 Goal/design。 |
| R3-CAP-005 | evidence and confidence pattern | `/tmp/interview-coach-skill/SKILL.md` 的 `Evidence enforcement`；`/tmp/interview-coach-skill/references/evidence-sourcing.md`；`/tmp/interview-coach-skill/references/commands/prep.md` 的 company/interviewer grounding rules；`/tmp/interview-coach-skill/references/commands/research.md`。 | 已有强落点：`apps/api/app/application/polish/question_generation_service.py::QuestionGenerationService.generate` 构造 `evidence_refs`、`low_confidence_flags`、`source_availability`、`grounding_status`；`apps/api/app/application/polish/question_generation_prompts.py::build_question_provider_request` 传入 compact `canonical_evidence` 并要求 `use_only_listed_evidence_refs`；`apps/api/app/application/polish/feedback_prompt_assets.py::build_feedback_prompt_asset` 要求 evidence items、citation rules、raw prompt/provider payload 禁止；`apps/api/app/application/polish/feedback_validation.py::validate_final_feedback_payload` 要求 `trace_refs`；schema `apps/api/app/schemas/polish.py::PolishFeedbackPayload` 有 `evidence_refs`、`trace_refs`、`low_confidence_flags`；docs `docs/02-design/PROMPT_SPEC.md`、`SCORING_SPEC.md`、`SEMANTICS_GLOSSARY.md` 已登记 evidence/low confidence 语义。差距是 company/interviewer live research 不是当前默认功能。 | Adapt | 可吸收“证据绑定、低置信、缺证停写”的模式，且与当前 prompt/validation/schema/doc 方向一致；但不吸收外部 company research claims。 | Round 4 候选 Goal 包：`R4-GP-evidence-confidence`，本轮不创建文件。 | 后续设计必须逐条绑定 `evidence_refs`、`trace_refs`、`low_confidence_flags`、source availability、validation errors 和 UI 可见行为；不得把未证实 company/interviewer claim 作为输出。 |
| R3-CAP-006 | rubric-plus-root-cause pattern | `/tmp/interview-coach-skill/references/rubrics-detailed.md` 的 five-dimension scoring rubric、root-cause taxonomy、seniority calibration bands；`/tmp/interview-coach-skill/references/transcript-processing.md` 的 `## Step 3: Multi-Lens Scoring` 与 Root Cause 输出；`/tmp/interview-coach-skill/references/calibration-engine.md` 的 `## Section 3: Cross-Dimension Root Cause Tracking`。 | 当前 scoring/feedback 落点存在但语义不同：`apps/api/app/domain/polish/policies/scoring_policy.py::ScoringPolicy.evaluate` 基于 loss points severity 输出 `score_type="polish_answer"` 和 0-100 score；`apps/api/app/schemas/polish.py::PolishFeedbackPayload` 包含 `score_result`、`scoring_dimensions`、`loss_points`、`feedback_cards`、`missing_answer_dimensions`、`technical_gaps`、`communication_gaps`；`docs/02-design/SCORING_SPEC.md` 规定 `polish_answer` 维度、`ScoreResult`、`evidence_refs`、`trace_refs`、low confidence；`apps/api/app/infrastructure/db/models/scoring.py::ScoreResult` 有 score metadata 与 `dimension_scores_json`、`evidence_links_json`。差距是没有 interview-coach 的 1-5 exact anchors、seniority bands 或 cross-session root-cause lifecycle。 | Adapt | 可吸收“分数必须连到原因和下一步”的结构，但必须映射到 AIForInterviewer 0-100 product scale、loss points、feedback_cards、ScoreResult 和 SCORING_SPEC，不复制 source 维度名或 anchors。 | Round 4 候选 Goal 包：`R4-GP-scoring-root-cause-alignment`，本轮不创建文件。 | 后续设计必须明确 score scale、score_type、rubric_version、loss point evidence、root-cause candidate 是否只是 candidate；不得引入精确通过概率或覆盖 `SCORING_SPEC.md`。 |
| R3-CAP-007 | storybank as reusable memory pattern | `/tmp/interview-coach-skill/references/storybank-guide.md` 的 `## Storybank Format`、`## Maintaining Your Storybank`、`## Story Selection Strategy`、`## Story Retirement Criteria`；`/tmp/interview-coach-skill/references/story-mapping-engine.md` 的 `## Section 1: Story-Question Fit Scoring`、`## Section 2: Portfolio Optimization Protocol`；`/tmp/interview-coach-skill/references/commands/stories.md`。 | 当前未发现 `Storybank` / `Story` 一级模型、repository、API 或 UI。可相邻参考的只有 `Asset`、`Weakness`、`TrainingRecommendation` 设计与候选确认流，以及 `apps/api/app/application/polish/feedback_prompt_assets.py::build_feedback_prompt_asset` 中 `project_asset_summaries`、`canonical_project_assets`；但这不是 storybank 落点。当前行为没有 story usage count、freshness、retirement、question mapping。 | Defer | 能力可能适合未来 Asset/experience memory，但当前 landing point 不够明确；按硬性约束不能标记 Adopt/Adapt。 | 无；Round 4 默认不创建 storybank Goal。 | 后续如要进入范围，先做 Asset/Weakness/Training 与 story model 的 active-doc/code audit；未完成前不得新建 storybank 表、API 或 prompt。 |
| R3-CAP-008 | progress calibration pattern | `/tmp/interview-coach-skill/references/commands/progress.md` 的 progress review thresholds；`/tmp/interview-coach-skill/references/calibration-engine.md` 的 `## Section 2: Scoring Drift Detection Protocol`、`## Section 6: Learning from Successes`、`## Section 7: Structured Unmeasured Factor Investigation`；`/tmp/interview-coach-skill/references/commands/feedback.md` 的 outcome/drift triggers。 | 当前可观察落点只有 `progress_tree_state`、`ScoreResult`、feedback payload、report summary。`apps/api/app/infrastructure/db/models/scoring.py::ScoreResult` 可存 score metadata；`apps/api/app/infrastructure/db/models/interview.py::PolishSessionDetail` 可存 progress state；但当前未发现 outcome log、external feedback store、self-assessment delta、real outcome correlation 或 scoring drift lifecycle。 | Defer | 当前只支持 session progress/status 与 feedback-level next actions，不能声称有 outcome calibration。 | 无；Round 4 默认不创建 calibration Goal。 | 后续若进入范围，必须先定义 outcome input、self-assessment、score history query、drift rules、privacy policy 和 tests；不能只基于 current progress tree 推断 calibration。 |
| R3-CAP-009 | state-aware next-step pattern | `/tmp/interview-coach-skill/SKILL.md` 的 session start recommendation logic；`/tmp/interview-coach-skill/references/commands/analyze.md` 的 `## Recommended Next Step`；`/tmp/interview-coach-skill/references/commands/progress.md` 的 progress-based next action；`/tmp/interview-coach-skill/references/story-mapping-engine.md` 的 gap / priority output。 | 当前落点明确：`apps/api/app/schemas/polish.py::PolishFeedbackPayload` 有 `next_recommended_actions`；`apps/api/app/application/polish/feedback_validation.py::validate_final_feedback_payload` 校验 `_next_recommended_actions`；`docs/02-design/prompt-contracts/POLISH_CONTRACTS.md` 规定 `next_recommended_actions` 只表达建议动作，不直接写正式对象；前端 `apps/web/src/pages/interview/InterviewPage.tsx::InterviewWorkbenchPage` 使用 `buildWorkbenchFixedNextActionBarViewModel`、`deriveWorkbenchQuestionActionState`、`deriveComposerActionViewModel`、progress tree current priority 和 selected progress node 驱动下一步 UI。差距是当前 next action 是 feedback/workbench 级，不是完整 session-start prescriptive coach。 | Adapt | 可吸收“下一步由状态驱动”的模式，但保持现有 next action enum、progress tree 和用户确认边界，不复制 command recommendation logic。 | Round 4 候选 Goal 包：`R4-GP-state-aware-next-actions`，本轮不创建文件。 | 后续设计必须说明 action 来源、枚举、UI 入口、不会自动写 formal object、与 progress node/current question 的关系，并补 API/frontend tests。 |
| R3-CAP-010 | long-context hygiene pattern | `/tmp/interview-coach-skill/references/archival-rules.md`；`/tmp/interview-coach-skill/SKILL.md` 的 `### Archival Rules`；`/tmp/interview-coach-skill/references/coaching-state-schema.md` 的 `Historical Summary` / `Recent Scores` / `Recent Sessions` sections。 | 当前已有 context bounding 但没有 archival：`apps/api/app/application/polish/question_generation_prompts.py::build_question_provider_request` 限制 evidence summaries、missing context、follow-up refs 并携带 `dropped_context_summary`；`apps/api/app/application/polish/feedback_prompt_assets.py::build_feedback_prompt_asset` 限制 `current_answer.answer_text_max_chars`、标记 `full_answer_forbidden`，压缩 recent turns / job / resume / assets；`docs/02-design/APPLICATION_FLOW_SPEC.md` 指定 feedback 输入包括 `SessionSummary`、`recent_turns_summary` 和 `trace_refs`；`docs/02-design/PROMPT_SPEC.md` 禁止把整场模拟面试塞进一个 Prompt。差距是没有持久化 archival summary thresholds。 | Adapt | 可吸收“prompt/context 要有 bounded input 与摘要”的模式；但不引入 source 的 state archival tables，先对齐现有 prompt compacting 和 `SessionSummary` 设计。 | Round 4 候选 Goal 包：`R4-GP-context-hygiene`，本轮不创建文件。 | 后续设计必须列出 token/char/turn 限制、summary/dropped_context 字段、不得存 raw provider payload、对应 tests；不创建未经授权的 archive/migration。 |

## 4. Reject 清单

| 项 | 决策 | 依据 |
| --- | --- | --- |
| 直接复制 interview-coach command names、menus、workflow wording、output wording | Reject | Round 1 非复制建议；AIForInterviewer 当前入口是 API/workbench，不是 agent command bundle。 |
| 直接复制 `/tmp/interview-coach-skill/references/coaching-state-schema.md` 的 flat `coaching_state.md` | Reject | AIForInterviewer 当前事实源是 SQLAlchemy model、repository、API schema 和 frontend type。 |
| 直接复制 interview-coach scoring dimensions、1-5 anchors、seniority bands 或 hire-signal wording | Reject | AIForInterviewer `SCORING_SPEC.md` 与 current code 使用 0-100 product scale、`polish_answer` score type、loss points 和 evidence/trace 语义。 |
| 直接复制 company/interviewer claim examples 或 prompt prose | Reject | AIForInterviewer 已有 source availability、evidence binding、privacy and prompt boundary；未证实外部 claim 不得进入输出。 |

## 5. 本轮结论

- Adopt: 暂无。当前可复用项均需改写到 AIForInterviewer 的现有边界，因此标记为 Adapt，而不是直接 Adopt。
- Adapt: `R3-CAP-002`、`R3-CAP-003`、`R3-CAP-005`、`R3-CAP-006`、`R3-CAP-009`、`R3-CAP-010`。
- Defer: `R3-CAP-004`、`R3-CAP-007`、`R3-CAP-008`。
- Reject: 直接复制 command/menu/prose、flat state、source scoring vocabulary、company/interviewer examples。
- Research-only: `R3-CAP-001`。
