---
title: F4_TO_F8_READINESS_RISK_REGISTER
type: risk-register
status: active-f4
source_task: AIFI-ARCH-005
permalink: ai-for-interviewer/design/reviews/f4-to-f8-readiness-risk-register
---

# F4→F8 交接就绪性风险登记表

状态：Pending

本文件登记 `AIFI-ARCH-005` 的 F4→F8 严格审计风险。审计产物已通过 Dashboard 审批；后续整改允许将单个 finding 更新为 `Fixed`，但 `Fixed` 不等于 `Verified`，也不代表 `F4_TO_F8_READINESS_ACCEPTANCE.md` Accepted。

| ID | 严重度（Severity） | 类别（Category） | 影响阶段 | 来源 | 风险 | 必要修复 | 状态 | Owner | 关闭条件 |
|---|---|---|---|---|---|---|---|---|---|
| AR-F4-F8-001 | High | API / Data / F5 Backend / F6 Frontend / F7 QA | F5 / F6 / F7 | `API_SPEC.md` §6-§8；`DATA_MODEL.md` §4.4、§11.2、§14 | `API_SPEC.md` 只有 endpoint matrix 和对象名级响应，缺逐接口字段级 contract；`DATA_MODEL.md` 缺幂等持久化承接 | 在 `API_SPEC.md` 增加 API 清单、逐接口详情、字段级 schema、headers、error responses、examples；在 `DATA_MODEL.md` 增加 `IdempotencyRecord` 或等价对象 | Verified | F4 Architecture / API / Data owners | Verified：本轮前定向复核确认当时 49 个核心 endpoint 均有字段级 contract 和 F7 assertion；本轮人工语义修复已调整路由清单，新增 `AR-F4-F8-004` / `005` / `006` 等待定向复核 |
| AR-F4-F8-002 | High | F6 Frontend / API / Governance | F6 / F7 / F8 | `API_SPEC.md` §6.1-§6.6；`TECH_DESIGN.md` §15.1；`UX_SPEC.md` §5.17；`UI_DESIGN_SYSTEM.md` §18-§19 | 缺页面到 API / response / view model / 状态 / 错误态 / confirmation / copy boundary 的 F6 接入矩阵 | 在 `API_SPEC.md` / `TECH_DESIGN.md` 增加 F6 Page/API Handoff Matrix，并为候选确认、错误态、空态、低置信度、source unavailable、stale version、copy boundary 定义页面规则 | Verified | F4 Architecture / API / Frontend owners | Verified：定向复核确认 `API_SPEC.md` §6.1 覆盖 32 个 F6 page / surface 且无空列，§6.2 覆盖 19 个状态和 N/A 规则，§6.3-§6.6 覆盖前端字段族、confirmation flow、copy boundary、no export 禁止项和 F2/F3 待补缺口；`TECH_DESIGN.md` §15.1 建立 F4→F6 handoff 规则 |
| AR-F4-F8-003 | High | F8 Release / Security / Governance | F8 | `DELIVERY_PLAN.md` §1；`BACKLOG.md` §1；`SECURITY_PRIVACY.md` §12、§22；`API_SPEC.md` §11；`DATA_MODEL.md` §12 | F4 缺 release checklist 来源矩阵、runbook、known limitations、rollback / migration、observability、provider failure 和 Deferred→Backlog 映射 | 在 `TECH_DESIGN.md`、`SECURITY_PRIVACY.md`、`API_SPEC.md`、`DATA_MODEL.md` 和必要的 `BACKLOG.md` 中补 F8 release handoff、发布前检查、运维隐私、回滚迁移和 Deferred 承接 | Open | F4 Architecture / Security / Release owners | F8 可从 active docs 直接生成 release checklist、known issues、runbook、rollback strategy、changelog 输入和下一轮 Backlog |
| AR-F4-F8-004 | High | API / Data / Product Semantics | F5 / F6 / F7 | 人工审计决策；`PRD.md` §6.1、§6.5；`UX_SPEC.md` §5.2 | Resume API 与 Markdown-only 产品口径不一致，错误引入 `modules[]` 和 project-experience module CRUD | 删除或废弃 Resume project-experience module API；Resume CRUD 仅返回 Markdown 正文、版本、状态和摘要；`DATA_MODEL.md` 将项目经历降级为 Markdown 片段 / derived outline | Verified | F4 Architecture / API / Data owners | Verified：本轮定向复核确认 API 清单、逐接口详情、Schema 索引、F6 matrix、F7 assertions、DATA_MODEL、TECH_DESIGN、SECURITY_PRIVACY 和 Prompt contracts 均不再把项目经历实现为 module CRUD / `modules[]` / 独立 ResumeModule owner-version-CRUD |
| AR-F4-F8-005 | High | API / F6 Frontend / Product Semantics | F6 / F7 | 人工审计决策；`UX_SPEC.md` §5.3-§5.4；`UI_DESIGN_SYSTEM.md` §18 | Job list/detail 缺少绑定简历摘要和最新岗位-简历匹配度摘要，无法支撑 F6 岗位页面 | `GET /api/v1/jobs` 与 `GET /api/v1/jobs/{job_id}` 增加 `binding_summary` 和 `latest_match_summary`，并在 DATA_MODEL 中确认由 `JobResumeBinding` / `JobMatchAnalysis` 派生 | Verified | F4 Architecture / API / Frontend owners | Verified：本轮定向复核确认 Job list/detail 直接返回 `binding_summary` / `latest_match_summary`，Schema 索引覆盖 `JobBindingSummary` / `JobMatchSummary`、状态域、评分刻度 / 版本 / rubric、stale 原因和 F6/F7 断言，且不返回精确通过概率 |
| AR-F4-F8-006 | High | API / Prompt / F6 Frontend / Product Semantics | F5 / F6 / F7 | 人工审计决策；`UX_SPEC.md` §5.7；`PROMPT_SPEC.md` §5；`POLISH_CONTRACTS.md` §12 | Polish mode API 缺少主题/次主题选择 contract，且 `binding_id` 语义不清 | Polish session request 使用 `resume_job_binding_id`，新增 topic/subtopic/custom topic 字段和 `GET /api/v1/polish-topics`；DATA/PROMPT/Polish contracts 增加 `PolishTopicRef` / `PolishSubtopicRef` 与安全输入边界 | Verified | F4 Architecture / API / Prompt owners | Verified：本轮定向复核确认 Polish request 使用 `resume_job_binding_id`，topic/subtopic API、schema、F6 surface、F7 tests、Prompt context assembly 和 `custom_topic_text` prompt injection 防护均已登记 |
| AR-F4-F8-007 | Medium | Governance / Documentation / F5-F8 Handoff | F5 / F6 / F7 / F8 | 人工审计；`AGENTS.md`；`docs/02-design` 正式设计文档 | `docs/02-design` 正式设计文档英文正文、章节标题、表头和说明过多，违反中文优先文档治理要求，并降低 F5-F8 交接可读性 | 将正文、章节标题、表头、风险、验收和测试描述改为中文优先；保留 API path、JSON 字段、enum、schema、ID、命令和文件路径等技术标识符 | Verified | 文档治理 / F4 Architecture owners | Verified：本轮定向复核未发现指定纯英文标题；中文主标题 + 英文括注、API path、JSON 字段、Header、enum、schema、API ID、assertion id 和验证命令保留原样；AR-F4-F8-008 的技术标识符不可变性仍保持 |
| AR-F4-F8-008 | High | API / Governance / F5-F8 Handoff | F5 / F6 / F7 | 人工审计；`AGENTS.md`；`API_SPEC.md` §6-§10 | 中文化 remediation 误改 API path / 技术标识符，破坏 API 清单和逐接口 contract | 恢复 API path、path params、JSON 字段、Header、enum、schema 名、API ID 和 F7 assertion id 的原样性；对齐 API 清单总表与逐接口 Method + Path | Verified | F4 Architecture / API / Governance owners | Verified：本轮定向复核确认 `API_SPEC.md` API path 字段内中文、pipe、空格和中文说明污染均为 0；API 清单总表与逐接口详情 Method + Path 均为 48 且集合差异为 0；API ID 唯一；report sections endpoint 和 project-experience module CRUD 均不存在；JSON 字段、Header、enum、schema 名、API ID、F7 assertion id 未被中文误翻译 |

## 实际修复摘要

| ID | 实际修复摘要 | 复核状态 |
|---|---|---|
| AR-F4-F8-001 | `API_SPEC.md` 新增 API 清单总表、逐接口字段级详情和 Schema 索引，逐接口展开 path/query/header/body/success/error/F7 assertions；`DATA_MODEL.md` 新增 `IdempotencyRecord`、`AiTask` / `AiTaskResult`、`ApiRequestTrace` / `TraceRef`、`AuditEvent` 覆盖范围和 persistence handoff，承接幂等、AI task、trace、candidate/suggestion/formal object 边界。注：本轮人工审计语义修复已调整路由清单，后续由 `AR-F4-F8-004` / `005` / `006` 定向复核。 | Verified；定向复核通过，整体验收仍为 Pending |
| AR-F4-F8-002 | `API_SPEC.md` 新增 F6 Page/API Handoff Matrix，覆盖 32 个 F6 page / surface、19 个状态、所需 API、响应 schema、加载 / 空态 / 错误 / 权限 / 候选 / 复制状态、F7 assertions、前端字段需求、用户确认流、F6 禁止能力和 F2/F3 一致性待补缺口；`TECH_DESIGN.md` 新增 F4→F6 handoff 规则，明确 F6 不得从 UX/UI 反向发明 API 字段、状态或错误码。 | Verified；定向复核通过，整体验收仍为 Pending，`AR-F4-F8-003` 仍为 Open |
| AR-F4-F8-004 | `API_SPEC.md` 删除 Resume project-experience module CRUD、`modules[]` 和基础 CRUD `source_availability`；`DATA_MODEL.md` 将项目经历降级为 Markdown 片段 / derived non-persistent outline，并禁止独立 version / owner / CRUD；`TECH_DESIGN.md` 同步 Markdown-only handoff；`SECURITY_PRIVACY.md` 和 Prompt contracts 收敛为 `ResumeMarkdownOutline` / Markdown 位置范围。 | Verified；定向复核通过，整体验收仍为 Pending |
| AR-F4-F8-005 | `API_SPEC.md` 为 Job list/detail 增加 `JobBindingSummary`、`JobMatchSummary`、summary 状态域、`score_scale` / `score_version` / `rubric_version` 和 no exact probability 约束；`DATA_MODEL.md` 确认 summary 由 `JobResumeBinding` / `JobMatchAnalysis` 派生，不新增物理实现细节。 | Verified；定向复核通过，整体验收仍为 Pending |
| AR-F4-F8-006 | `API_SPEC.md` 将 Polish request 的 `binding_id` 收敛为 `resume_job_binding_id`，增加 `topic_id`、`subtopic_id`、`custom_topic_text` 和 `GET /api/v1/polish-topics`；`DATA_MODEL.md`、`PROMPT_SPEC.md`、`POLISH_CONTRACTS.md`、`SECURITY_PRIVACY.md` 增加 topic/subtopic ref、上下文装配和 prompt injection 防护。 | Verified；定向复核通过，整体验收仍为 Pending |
| AR-F4-F8-007 | `docs/02-design` 正式设计文档和 AIFI-ARCH-005 审查产物改为中文优先表达：API 逐接口标题 / 表头 / 固定字段标签、Schema 索引、Prompt contract 模板标签、审查 finding / 风险 / 修复 / 验收说明均已中文化；API path、JSON 字段、enum、schema、ID、命令和文件路径保留原样。 | Verified；定向复核通过，整体验收仍为 Pending |
| AR-F4-F8-008 | `API_SPEC.md` 恢复并复核 API path / 技术标识符原样性：移除未登记的 report sections endpoint 说明并改为 `data.sections[]`；修正被中文化括注污染的 API 名称；机械对账确认 API 清单总表与逐接口详情均为 48 个 Method + Path，且 project-experience module CRUD 未恢复。 | Verified；定向复核通过，整体验收仍为 Pending，`AR-F4-F8-003` 仍为 Open |
