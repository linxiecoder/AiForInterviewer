---
title: F4 Full Design Remediation Matrix
type: review
status: active-f4
source_task: AIFI-ARCH-004
permalink: ai-for-interviewer/design/reviews/f4-full-design-remediation-matrix
---

# F4 全量技术设计整改矩阵

## 1. 使用边界

本矩阵描述 `AIFI-ARCH-004` 的建议整改动作，并在后续授权 remediation 中记录实际回写结果。所有整改必须在授权轮次回写到对应 active docs；本文件不替代 `TECH_DESIGN.md`、`DATA_MODEL.md`、`SECURITY_PRIVACY.md`、`API_SPEC.md`、`PROMPT_SPEC.md` 或 `prompt-contracts/*.md`。

## 2. Remediation Matrix

| Finding | Target Active Doc | Required Change | Verification Method | Blocks F4 Exit | Blocks F5 | Blocks F7 | Status |
|---|---|---|---|---|---|---|---|
| AR-F4-FULL-001 | `TECH_DESIGN.md`; `DATA_MODEL.md`; `API_SPEC.md`; `PROMPT_SPEC.md`; `SECURITY_PRIVACY.md`; `prompt-contracts/*.md` | 逐项梳理 `F4_TECH_DESIGN` UNKNOWN，分类为 `must_close_in_F4`、`already_closed_by_recent_remediation`、`deferred_non_blocking`、`false_positive` 或 `requires_human_decision`；本轮不改 `BACKLOG.md` | 本轮 verification 已复核 `F4_TECH_DESIGN` / `UNKNOWN` / Open Questions 命中，确认无 must_close_in_F4 未关闭项；F7 可形成 UNKNOWN closure fixture | No | No | No | Verified |
| AR-F4-FULL-002 | `API_SPEC.md`; `TECH_DESIGN.md`; `SECURITY_PRIVACY.md` | 补齐 endpoint、method、path、request / response、error code、auth / owner、pagination / sorting / filtering、status query、async / sync、retry、idempotency、timeout、rate limit；同步 API handoff 当前性 | API contract review；targeted F7 API tests for auth/owner/failure/retry/rate limit/source unavailable/low confidence/no export/copy boundary | No | No | No | Verified |
| AR-F4-FULL-003 | `TECH_DESIGN.md`; `DATA_MODEL.md`; `API_SPEC.md`; `PROMPT_SPEC.md`; `SECURITY_PRIVACY.md`; `JOB_MATCH_CONTRACTS.md`; `REPORT_CONTRACTS.md`; `SHARED_CONTRACTS.md`; `POLISH_CONTRACTS.md`; `PRESSURE_CONTRACTS.md` | 冻结 0-100 产品评分刻度、rubric / rule version、版本字段、risk wording、低置信触发、免责声明、禁止精确概率和 F7 断言；采用 rubric / rule version，真实结果校准后置 | 本轮 verification 已复核 score range、version、confidence、risk + evidence、low confidence、source unavailable、validation failed、no probability、copy boundary；`P-POLISH-004` / `P-PRESSURE-008` 的 remaining gaps 已关闭 | No | No | No | Verified |
| AR-F4-FULL-004 | `REPORT_CONTRACTS.md`; `API_SPEC.md`; `PROMPT_SPEC.md`（只读核对，无需改动）; `SECURITY_PRIVACY.md`（只读核对，无需改动） | 删除 / 改写 `download_markdown`、`MarkdownExportSnapshot`、`download_filename_hint`、download / export 语义；收敛为页面复制；API 只保留读取和复制内容边界 | `rg -n "download|export|下载|导出|filename|MarkdownExport"` active docs；F7 no file export test | No | No | No | Verified |
| AR-F4-FULL-005 | `DATA_MODEL.md`; `API_SPEC.md`; `PROMPT_SPEC.md`; `SHARED_CONTRACTS.md`; `PRESSURE_CONTRACTS.md` | 冻结 pause / resume 最小快照、进展树状态机、status query、cancel、retry、timeout、partial result 和 source unavailable 恢复路径 | State-machine fixture 覆盖 pause/resume/resume failed/timeout/partial/source unavailable/low confidence inherited | No | No | No | Verified |
| AR-F4-FULL-006 | `DELIVERY_PLAN.md`; `UI_DESIGN_SYSTEM.md`; F6 task handoff docs if authorized | 统一 F3 DONE 与 UI 高保真 UNKNOWN / NOT_STARTED 的口径；明确 F6 使用哪些 approved design input | `rg -n "F3 |UNKNOWN|NOT_STARTED|APPROVED"`；人工设计交接复核 | No | No | Yes | Deferred |
| AR-F4-FULL-007 | `POLISH_CONTRACTS.md`; `PRESSURE_CONTRACTS.md`; `REPORT_CONTRACTS.md`; `REVIEW_CONTRACTS.md`; `WEAKNESS_CONTRACTS.md`; `ASSET_CONTRACTS.md`; `PROMPT_SPEC.md` | 更新过期的“仍保持 Stub”关系说明，与当前 Draft registry 对齐，同时保留 UNKNOWN / non-write 边界 | `rg -n "仍保持 Stub|仍是 Stub|保持 Stub"`；Prompt registry / child status diff | No | No | No | Verified |

## 3. Suggested Remediation Order

1. P0: `AR-F4-FULL-004` 已在本轮 Verified，后续只需在 F5 / F7 按 no file export test 回归。
2. P0: `AR-F4-FULL-002` 已 Verified；后续 F5 / F7 只需按 API contract tests 防止实现偏离。
3. P0: `AR-F4-FULL-003` 已在本轮 verification 通过；后续仅需在 F5 / F7 以 fixture 防止实现偏离。
4. P0: `AR-F4-FULL-001` 已 Verified；后续 F5 / F7 只需按 UNKNOWN closure fixture 防止实现或文档回退。
5. P1: `AR-F4-FULL-005` 已 Verified；后续 F5 / F7 按 pause / resume state-machine fixture 回归。
6. P1: `AR-F4-FULL-006` 已 Deferred；F6 页面实现前必须完成 F3 / F6 设计交接状态同步或人工 handoff gate。
7. P2: `AR-F4-FULL-007` 已 Verified；后续只需防止子文档关系说明回退为过期 Stub 状态。

## 3.1 Actual Remediation Record

| Finding | Target Active Doc | Required Change | Actual Change | Verification Method | Status |
|---|---|---|---|---|---|
| AR-F4-FULL-001 | `TECH_DESIGN.md`; `DATA_MODEL.md`; `API_SPEC.md`; `PROMPT_SPEC.md`; `SECURITY_PRIVACY.md`; `prompt-contracts/*.md` | 将 F4 阻断式 UNKNOWN 逐项改为已冻结、已处置、`deferred_non_blocking`、`false_positive` 或 `requires_human_decision`；不得只在 review 文件说明；不得改 Medium / Low finding 状态；不得标记整体 Accepted | `TECH_DESIGN.md` 已新增 F4 UNKNOWN 收敛与后置边界表，明确评分、API、数据结构、Prompt、模型结果状态、安全、candidate / formal、资产、真实面试、进展树 / 暂停恢复、题目推荐、复盘切分、薄弱项生命周期和 non-goal 的分类。`DATA_MODEL.md` 已将 `DM-UNK-*` 改为 F4 待决策项处置表，冻结版本、状态、trace / evidence、candidate / formal、复盘切分和最小恢复 / 进展边界。`API_SPEC.md` 已明确 endpoint、error envelope、async task、retry、idempotency、rate limit、permission、copy boundary 和 F7 assertion 已交接。`PROMPT_SPEC.md` 和 `prompt-contracts/*.md` 已将阻断式 Open Questions 改为已冻结边界或 deferred_non_blocking。`SECURITY_PRIVACY.md` 已明确 owner、visibility、LLM input minimization、provider payload、system prompt、hidden scoring、日志、trace、copy boundary、third-party privacy 和 Deferred 安全边界。未修改 `BACKLOG.md` / `DOCS_INDEX.md`，未处理 Medium / Low，未进入 implementation。 | 本轮 verification 已确认：阻断短语无命中；剩余 `UNKNOWN` 仅为历史输入、处置标题或审计记录；`must_close_in_F4` 均有已冻结设计结论；Open Questions 均为 deferred_non_blocking、已冻结边界、候选 / 用户确认语义或 Stub 模板；F5 / F6 / F7 可基于当前 API / DATA / Prompt / Security 形成实现与测试输入。 | Verified |
| AR-F4-FULL-002 | `API_SPEC.md`; `TECH_DESIGN.md`; `SECURITY_PRIVACY.md` | 将 `API_SPEC.md` 从骨架补齐为 F5/F6/F7 可交接 API contract；必要时同步 API handoff 当前性 | `API_SPEC.md` 已补齐全局 API 约定、response / error envelope、pagination / sorting / filtering、idempotency、rate limit、trace / request id、async task protocol、AI task create / status / result / retry / cancel、核心 endpoint matrix、candidate / suggestion / confirmation 写入边界、报告 copy content 与 no export 边界、F7 可测试性矩阵。`TECH_DESIGN.md` 已同步 API handoff 当前性，不再说完整 endpoint / retry / idempotency 仍未收敛。`SECURITY_PRIVACY.md` 已同步 endpoint / API schema 由 `API_SPEC.md` 承接，本文不重复定义。未修改 `PROMPT_SPEC.md` 或 prompt-contracts 正文。 | 本轮 verification 已确认：`API_SPEC.md` 不再自称骨架或 endpoint 未定义；endpoint matrix 覆盖核心资源域；F7 assertion 覆盖 success、validation failed、cross-user、source unavailable、low confidence、generation failed、idempotent retry、stale conflict、no export endpoint、copy boundary；`TECH_DESIGN.md` / `SECURITY_PRIVACY.md` 无 API handoff 旧口径；禁止路径无变更。 | Verified |
| AR-F4-FULL-003 | `TECH_DESIGN.md`; `DATA_MODEL.md`; `API_SPEC.md`; `PROMPT_SPEC.md`; `SECURITY_PRIVACY.md`; `JOB_MATCH_CONTRACTS.md`; `REPORT_CONTRACTS.md`; `SHARED_CONTRACTS.md`; `POLISH_CONTRACTS.md`; `PRESSURE_CONTRACTS.md` | 冻结评分、通过倾向、风险提示和校准口径；明确 0-100 分数不是精确通过概率；补 score / rubric version、confidence、evidence、validation、low confidence、source unavailable、copy boundary 和 F7 断言 | `TECH_DESIGN.md` 已新增评分 / 倾向 / 风险 / 校准专项规则；`DATA_MODEL.md` 已补 `ScoreResult` 正式字段、rubric 维度、权重策略、版本变更解释、风险和通过倾向承载；`API_SPEC.md` 已补评分 / 风险响应语义、endpoint matrix 和 F7 断言；`PROMPT_SPEC.md` 已补 scoring candidate 全局规则；`SHARED_CONTRACTS.md` 已补 validation / low confidence / evidence binding；`JOB_MATCH_CONTRACTS.md` 与 `REPORT_CONTRACTS.md` 已补评分、风险、通过倾向 schema / validation / fallback；`SECURITY_PRIVACY.md` 已补隐藏规则、provider payload、第三方隐私和 copy content 边界。本轮 verification 已确认 `P-POLISH-004` 的 `score_version` / `rubric_version` / `confidence_level` / `evidence_refs` / `validation_status` / `generated_by_task_id` / `trace_refs` / `risk_level=not_applicable` 字段齐备，且只输出单轮 polish scoring candidate；`P-PRESSURE-008` 的 `score_version` / `rubric_version` / `confidence_level` / `evidence_refs` / `validation_status` / `generated_by_task_id` / `trace_refs` / `risk_level` / `risk_reason` 字段齐备，且只输出 pressure session scoring candidate / draft；两个 score contract 均不再保留 scoring UNKNOWN 开放项。未修改业务代码、未处理 AR-F4-FULL-001 / Medium / Low、未改 `BACKLOG.md` / `DOCS_INDEX.md`。 | 本轮 verification 已复核 0-100 score、禁止精确通过概率、low confidence 不输出确定倾向、source unavailable 报告降级、validation failed 不落正式报告评分、不暴露隐藏评分规则、risk_level 与 evidence_refs 同步存在、score_version / rubric_version 存在、copy content 不含内部评分规则，结论为 Verified。 | Verified |
| AR-F4-FULL-004 | `REPORT_CONTRACTS.md`; `API_SPEC.md` | 将 Report contract 的 Markdown 下载 / 文件产物语义收敛为报告内容复制 / copy content；API 不提供下载 / 导出 endpoint | `REPORT_CONTRACTS.md` 已移除公共 action 和 `P-REPORT-004` 中的文件生成动作、下载快照、文件名 hint、复制 / 下载混合校验和测试语义，改为剪贴板可用内容；已明确复制内容不得包含 `system prompt`、provider payload、隐藏评分规则和未经脱敏的第三方 / 公司 / 个人敏感信息。`API_SPEC.md` 已补充报告 API 只提供报告读取、报告分项读取和复制所需内容，不提供文件下载 / 导出 endpoint。 | `REPORT_CONTRACTS.md` 对 `download|export|下载|导出|filename|MarkdownExport` 无命中；`API_SPEC.md` 命中为“不提供下载 / 导出 endpoint”禁止说明；`SECURITY_PRIVACY.md` 命中为页面复制、Deferred 或 non-goal 边界；未发现 archive、snapshot file、report file、markdown artifact 或 downloadable content 等替代导出命名。 | Verified |
| AR-F4-FULL-005 | `DATA_MODEL.md`; `API_SPEC.md`; `SHARED_CONTRACTS.md`; `PRESSURE_CONTRACTS.md` | 冻结打磨 / 压力面 pause / resume 最小快照、进展树状态机、status query、cancel、retry、timeout、partial result 和 source unavailable 恢复路径 | `DATA_MODEL.md` 已明确打磨 / 压力面共享最小恢复快照、`source_session_snapshot_ref`、`covered_turn_refs`、`ProgressPosition`、恢复失败、来源不可用、partial 和低置信度继承；`API_SPEC.md` 已补压力面 pause / resume / end / mark_resume_failed endpoint，并已有 async task status / retry / cancel / timeout；`SHARED_CONTRACTS.md` 已定义 summary failure、pause snapshot unavailable、resume failed、low confidence inherited；`PRESSURE_CONTRACTS.md` 已将 pause / resume 仅作为建议动作或流程入口。 | 本轮 verification 已复核 DATA / API / Shared / Pressure 命中，确认 F7 可覆盖 pause、resume、resume failed、summary missing、covered_turn_refs missing、source unavailable、重复恢复请求、取消生成、timeout、partial result 和 low confidence inherited；结论为 Verified。 | Verified |
| AR-F4-FULL-006 | `DELIVERY_PLAN.md`; `UI_DESIGN_SYSTEM.md`; F6 handoff gate | 统一 F3 DONE 与 UI 高保真 UNKNOWN / NOT_STARTED 的口径；明确 F6 使用哪些 approved design input | 本轮未修改 `DELIVERY_PLAN.md` 或 `UI_DESIGN_SYSTEM.md`，原因是它们不是本轮允许写入的 F4 active design docs。`UI_DESIGN_SYSTEM.md` §22 仍含 UNKNOWN / NOT_STARTED，§23 记录 Figma 已完成设计候选但待 active docs 闭环验收，§24 要求 UNKNOWN 台账可关闭。 | Deferred 到 F6 前端启动前的设计交接复核；不阻断 M4 或 F5 后端启动，但 F6 / F7 进入页面实现和视觉验收前必须区分 approved / candidate / UNKNOWN design input。 | Deferred |
| AR-F4-FULL-007 | `POLISH_CONTRACTS.md`; `PRESSURE_CONTRACTS.md`; `REPORT_CONTRACTS.md`; `REVIEW_CONTRACTS.md`; `WEAKNESS_CONTRACTS.md`; `ASSET_CONTRACTS.md`; `PROMPT_SPEC.md` | 更新过期的“仍保持 Stub”关系说明，与当前 Draft registry 对齐，同时保留 UNKNOWN / non-write 边界 | 已将 Polish / Pressure / Report / Review / Weakness / Asset 子文档中的过期 Stub 关系说明改为当前 Draft registry 事实；所有新增关系说明均保留候选 / 建议 / 用户确认边界，明确不得自动写正式对象，不关闭 deferred_non_blocking 细化项。 | `rg -n "仍保持 Stub|仍是 Stub|保持 Stub" docs/02-design/PROMPT_SPEC.md docs/02-design/prompt-contracts` 不再命中过期状态说明；主文件的 `Stub` 命中仅为状态定义、Stub 模板或历史变更记录。 | Verified |

## 4. 不建议整改方式

- 不建议在 review 文件中补写 active design truth。
- 不建议让 F5 自行发明 API schema、错误码、评分阈值或导出策略。
- 不建议把旧 `AIFI-ARCH-003` finding 直接复制成新结论。
- 不建议用 archive 或 Figma 候选稿替代 active docs 事实。
