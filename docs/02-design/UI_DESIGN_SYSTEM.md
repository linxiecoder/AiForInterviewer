# AiForInterviewer UI Design System

## 1. 文档状态

- 阶段：F3 高保真设计与设计系统。
- 任务：AIFI-UI-001。
- 里程碑：M3。
- 优先级：MUST。
- 文档状态：DRAFT。
- 本文档性质：F3 设计系统草案。
- 输入依据：
  - `docs/01-product/PRD.md`。
  - `docs/02-design/UX_SPEC.md`。
  - `docs/03-delivery/DELIVERY_PLAN.md`。
  - `docs/03-delivery/BACKLOG.md`。
  - Figma F2 Low Fidelity 分批审计结果。
- 边界声明：
  - 本文档不修改 PRD。
  - 本文档不替代 `UX_SPEC.md`。
  - 本文档不改变 `BACKLOG.md` / `DELIVERY_PLAN.md` 状态。
  - Figma 只作为 F2 低保真证据来源。
  - WARN / UNKNOWN / CONFLICT 不得写成已验证事实。

## 2. 设计目标

- 将 F2 低保真结果转化为 F3 高保真设计系统草案。
- 支撑后续高保真页面设计，使页面视觉、布局、组件和状态表达有统一草案。
- 支撑前端组件实现，降低 F6 阶段页面实现时的组件重复和交互分歧。
- 服务 F2 已冻结的 PRD 核心需求，尤其是简历、岗位 / JD、岗位匹配分析、打磨模式、压力面模式、报告、复盘、薄弱项、资产库、训练建议和反馈回流。
- 防止 Figma 低保真中的不完整内容或冲突内容进入默认规范；所有不确定项必须进入 WARN / UNKNOWN / CONFLICT 台账。

## 3. 设计原则

- 真实岗位准备优先：页面、组件和状态必须围绕真实简历、真实岗位 / JD、岗位匹配、模拟面试和复盘沉淀展开。
- 不承诺精确通过概率：允许展示评分、风险提示和改进方向，不得展示未经 F4 / F7 关闭的精确通过概率。
- 不提供文件导出：MVP 不提供 PDF、Markdown 文件、Word / docx 或批量导出入口。
- 报告内容支持复制：复制是页面内交互能力，不等同于文件生成或下载。
- 岗位 / JD 以手动录入为主：F3 默认只承接手动录入表单，不设计外部文件解析生成岗位路径。
- 项目经历归入简历模块，不作为一级业务对象。
- 页面、组件、状态、交互分层沉淀，页面规范不得替代组件规范，组件规范不得替代产品需求。
- 组件优先复用，不为单页重复造组件。
- Figma 不覆盖 active docs；如 Figma 审计结果与 active docs 冲突，以 active docs 为准，并标记 CONFLICT。

## 4. 来源与证据边界

### 4.1 active docs 边界

- `docs/01-product/PRD.md` 是 MVP 产品需求唯一事实源。
- `docs/02-design/UX_SPEC.md` 是 F2 低保真设计唯一 active 文档。
- `docs/03-delivery/DELIVERY_PLAN.md` 是唯一阶段与里程碑入口。
- `docs/03-delivery/BACKLOG.md` 是唯一任务入口。
- 本文档只在 F3 范围内沉淀高保真设计系统草案，不反向修改上述入口。

### 4.2 Figma 证据边界

- Figma F2 Low Fidelity 只作为 F2 低保真证据来源。
- F2 低保真稿已被 active docs 登记为人工接受，可作为 F3 输入。
- F2 Low Fidelity Components 不是 F3 设计系统，只能作为 F2 低保真组件基线输入。
- Prototype Path 是低保真路径说明，不等于原生 Figma Prototype reactions。
- Figma `node.type` / Page 归属部分 UNKNOWN，不得写成确定事实。

### 4.3 低保真输入可继承内容

- App Shell、固定左侧导航、主内容区、全局用户区。
- 列表主态、右侧抽屉态、弹窗态、状态 Frame 替换内容区。
- 表格、分页、表头筛选 / 排序、操作列图标、最大承载状态。
- Markdown 编辑 / 预览、岗位手动录入、候选简历选择、内容沉淀确认、低置信校对。
- 打磨模式和压力面模式的四区工作台结构：当前模拟面试情况栏、进展树、聊天窗口、输入区。
- 报告复制、复盘新增、薄弱项多选发起打磨和资产库沉淀入口。

### 4.4 低保真输入不可直接继承内容

- 不能直接继承低保真灰阶为最终高保真色板。
- 不能直接继承低保真组件尺寸为最终设计 token。
- 不能把不完整字段、缺失动作或冲突语义写成默认规范。
- 不能把 F2 Low Fidelity Components 视为 F3 组件库完成态。
- 不能把 Figma Prototype Path 写成已完成原生原型交互。

### 4.5 UNKNOWN 处理规则

- 未被 active docs 或本轮可读 Figma 审计结论验证的字段、交互、状态和 Page 归属均标记 UNKNOWN。
- UNKNOWN 可作为 F3 待复核项进入草案，但不得写成完整、已验证或默认行为。
- UNKNOWN 若影响产品范围，应回到 PRD / UX_SPEC / BACKLOG / DELIVERY_PLAN 的对应变更窗口处理；本文档不自行关闭。

### 4.6 CONFLICT 处理规则

- 当 Figma 审计结果与 active docs 冲突时，以 active docs 为准。
- CONFLICT 不得进入默认规范。
- CONFLICT 必须进入 WARN / UNKNOWN / CONFLICT 台账，并注明后续由产品 / UX 或对应阶段复核。

## 5. 页面类型规范

| 页面类型 | 用途 | 默认规范 | 待复核标记 |
| --- | --- | --- | --- |
| 首页 / 工作台 | 汇总待办、最近记录、待复盘、薄弱项和推荐下一步 | 使用 App Shell + 卡片区 + 列表摘要；主操作指向简历、岗位、模拟面试和复盘 | 无 |
| 简历列表 | 管理简历入口 | 表格主态，支持筛选、排序、分页、操作列图标和空 / 加载 / 错误状态 | 无 |
| 简历详情抽屉 | 查看简历摘要、模块、绑定岗位和入口 | 右侧抽屉，底层列表弱化但可读，正文内部滚动 | 无 |
| 简历 Markdown 编辑抽屉 | 编辑 Markdown 简历并预览系统识别模块 | 编辑区 + 预览区 + 固定底部操作区 | 114:628 UNKNOWN，Markdown 编辑细节不足 |
| 岗位列表 | 管理岗位 / JD | 表格主态，新增岗位为页面主操作，筛选在表头 | 无 |
| 岗位详情与匹配分析抽屉 | 查看岗位详情、绑定简历和匹配分析 | 折叠模块承载岗位信息、绑定关系、匹配分析 | 114:1215 UNKNOWN，匹配分析细节不足 |
| 岗位手动录入抽屉 | 手动创建或编辑岗位 / JD | 表单抽屉，字段不得假设完整 | 114:1529 UNKNOWN，字段不足 |
| 模拟面试列表 | 管理打磨 / 压力面记录 | 表格主态，发起模拟面试为主操作，报告从列表 / 详情进入 | 无 |
| 模拟面试详情抽屉 | 查看模拟面试概要、进展、关联输入和报告入口 | 右侧抽屉，不作为独立一级页面 | 无 |
| 面试报告详情抽屉 | 查看评分、表现、失分点、建议、薄弱项和复制入口 | 报告详情抽屉，复制为页面内操作 | 114:2430 UNKNOWN，报告详情字段不足 |
| 发起模拟面试弹窗 | 选择模式、岗位、简历、薄弱项等输入后发起 | 弹窗或短流程抽屉；候选简历表格可嵌入 | 无 |
| 打磨模式工作台 | 支持同题多轮回答、点评、修改和反馈 | 四区布局，包含进展树、聊天窗口、反馈卡片和下一步建议固定区 | 无 |
| 压力面模式工作台 | 支持真实节奏连续问答 | 四区布局，但动作语义不得默认继承打磨模式 | 114:2930 CONFLICT 已解除 / M3 复核项 |
| 面试复盘列表 | 管理模拟面试复盘和真实面试复盘 | 表格主态，新增复盘为主操作 | 无 |
| 复盘报告详情抽屉 | 查看复盘结论、薄弱项、训练建议和沉淀入口 | 抽屉态，支持复制相关内容但不导出文件 | 114:3703 / 114:3754 UNKNOWN 关联 |
| 新增面试复盘弹窗 | 选择复盘类型并进入对应表单 | 弹窗，区分模拟面试复盘与真实面试复盘 | 114:3703 / 114:3754 UNKNOWN |
| 新增模拟面试复盘表单 | 从系统内模拟面试报告补充复盘 | 字段待复核，不得写成完整验证 | 114:3703 UNKNOWN |
| 新增真实面试复盘表单 | 用户录入外部真实面试经历 | 字段待复核，不得写成完整验证 | 114:3754 UNKNOWN |
| 资产库列表 | 管理可复用面试准备材料和复盘沉淀 | 表格主态，来源、类型、更新时间可筛选 | 无 |
| 资产详情抽屉 | 查看资产来源、正文、适用场景和关联 | 抽屉态，仅查看 / 编辑 / 关闭 / 回流关联查看 | 无 |
| 薄弱项列表 | 管理薄弱项、来源、严重度、状态和建议训练方式 | 表格主态，支持多选发起打磨 | 无 |
| 薄弱项详情抽屉 | 查看来源证据、训练建议和关联对象 | 抽屉态，关联入口不覆盖历史内容 | 无 |
| 薄弱项多选发起打磨弹窗 | 将多选薄弱项带入打磨模式发起 | 弹窗，展示已选项、目标岗位 / 简历和确认动作 | 无 |
| 候选简历选择表格 | 在岗位绑定或发起模拟面试中选择简历 | 表格组件变体，支持单选或多选待复核 | 单选 / 多选模式待复核 |
| 内容沉淀确认抽屉 | 用户确认内容是否沉淀到资产库、薄弱项、训练建议或后续输入 | 不自动静默写入，支持确认、编辑、取消目标、跳过和失败重试 | 复盘沉淀链路字段待复核 |
| 低置信度校对抽屉 | 让用户校对低置信信息 | 不伪造阈值，仅展示待校对项、原因、用户修正和保存状态 | 低置信度规则待 F4 / F7 |
| 用户设置抽屉 | 展示账号信息、偏好、通知、退出和管理员入口 | 从全局用户区打开，右侧抽屉或弹层 | 管理台完整能力不进入 F3 默认规范 |

## 6. 布局规范

- App Shell：由固定左侧导航、主内容区和全局用户区组成；页面内容不得绕过 App Shell 单独建立入口。
- 固定左侧导航：承载工作台、简历、岗位 / JD、资产库、模拟面试、面试复盘和薄弱项；用户区位于左侧导航底部或统一全局位置。
- 主内容区：承载页面标题、主操作、表格 / 卡片 / 工作台主体和状态 Frame。
- 全局用户区：展示头像、用户名或占位、角色、设置入口；管理员可见管理入口，但不展开完整管理台。
- 列表主态：页面主体为表格，主操作在列表上方，筛选和排序在表头。
- 右侧抽屉态：抽屉从右侧浮出，覆盖当前列表或页面；背景列表弱化但可读。
- 弹窗态：短流程使用弹窗，底部操作区固定，内容过长时内容区滚动。
- 表格最大承载状态：表格密度与分页语义一致，不能只画少量行却显示大量总数。
- 状态 Frame 替换内容区，不遮挡主体：空态、加载态、错误态替代表格区或内容区。
- 背景列表弱化但可读：抽屉打开时不得用实心遮罩抹除行、列、分页和当前位置。
- 固定底部操作区：抽屉和弹窗底部操作区固定，保存、取消、确认、关闭等动作位置稳定。

## 7. 基础组件规范

| 组件 | 用途 | 适用页面 | 低保真来源 | 状态 | F3 待补充项 |
| --- | --- | --- | --- | --- | --- |
| Button | 触发主操作、次操作、危险确认和表单提交 | 全部页面、抽屉、弹窗 | F2 弹窗、抽屉、表单、确认态 | 默认、hover、pressed、disabled、loading、danger | 高保真颜色、尺寸、层级和危险态视觉 |
| Icon Button | 表格操作、设置、关闭、筛选、排序、展开收起 | 表格、抽屉、弹窗、导航、进展树 | F2 操作列、表头图标、用户区 | 默认、hover、active、disabled、tooltip | 图标库、触达面积、焦点态 |
| Input | 单行文本输入 | 岗位录入、筛选搜索、设置、弹窗表单 | F2 表单与筛选下拉 | 默认、focus、filled、error、disabled、saving | 字段高度、错误样式、前后缀图标 |
| Textarea | 多行文本输入 | 岗位职责、岗位要求、其他、复盘内容、校对内容 | F2 岗位录入、复盘表单 | 默认、focus、filled、error、disabled、saving | 字数提示、自动高度、长文本滚动 |
| Select | 单选选择 | 模式选择、岗位、简历、状态、类型 | F2 发起模拟面试、表单 | 默认、open、selected、disabled、error | 搜索、多选差异、键盘交互 |
| Checkbox | 多选选择 | 薄弱项多选、筛选、多目标沉淀 | F2 筛选下拉、薄弱项多选、内容沉淀 | unchecked、checked、indeterminate、disabled | 多选承载上限和批量操作样式 |
| Form Item | 表单字段容器 | 全部表单、抽屉、弹窗 | F2 表单行 | 默认、required、error、hint、saving | 标签宽度、帮助文本、校验提示 |
| Modal | 短流程弹窗 | 发起模拟面试、新增复盘、确认操作 | F2 弹窗组件 | open、loading、error、success、confirm-close | 宽度分级、焦点锁定、遮罩样式 |
| Drawer | 详情、创建、编辑、确认、校对 | 简历、岗位、报告、复盘、资产、薄弱项、设置 | F2 抽屉组件 | open、saving、error、success、confirm-close | 宽度 token、滚动区、底部操作视觉 |
| Table | 列表和候选选择 | 简历、岗位、模拟面试、复盘、资产、薄弱项、候选简历 | F2 表格组件 | default、empty、loading、error、selected、max-density | 高保真行高、列宽、固定列、横向滚动 |
| Pagination | 列表分页 | 所有表格页 | F2 分页组件 | default、current、disabled、page-size-open | 视觉密度、页码省略规则 |
| Filter Dropdown | 表头筛选 | 所有列表表头 | F2 表头筛选下拉 | closed、open、searching、selected、empty、applied | 补齐应用 / 取消 / 重置 / 清空闭环 |
| Toast / Feedback | 操作结果反馈 | 全部页面 | F2 气泡组件 | success、error、warning、info | 位置、持续时间、非唯一反馈规则 |
| Card | 工作台摘要、报告模块、反馈模块 | 工作台、报告、工作台反馈 | F2 工作台卡片、反馈卡片 | default、hover、selected、loading | 视觉层级、阴影、内边距 |
| Accordion | 折叠模块 | 抽屉、报告、反馈卡片、详情模块 | F2 折叠模块 | expanded、collapsed、error-expanded | chevron 样式、标题层级、滚动定位 |
| Empty State | 无数据或未创建 | 列表、内容区、模块 | F2 状态 Frame | empty、no-permission、insufficient-data | 插画 / 图标风格、主次动作 |
| Loading Skeleton | 加载占位 | 表格、抽屉、报告、工作台 | F2 状态 Frame | row-loading、card-loading、drawer-loading | 骨架节奏、动效是否启用 |
| Error Retry | 错误恢复 | 列表、报告、抽屉、工作台 | F2 状态 Frame | error、retrying、failed-again | 错误等级、重试按钮和说明文案 |

## 8. 业务组件规范

| 业务组件 | 用途 | 组成 | 默认规范 | 待复核 |
| --- | --- | --- | --- | --- |
| Resume List | 简历管理 | Table、Pagination、Filter Dropdown、操作列 | 进入简历详情和 Markdown 编辑 | 无 |
| Resume Detail Drawer | 简历详情 | Drawer、Accordion、Button、Toast | 展示简历模块、绑定岗位和入口 | 无 |
| Resume Markdown Editor | Markdown 编辑 / 预览 | Drawer、Textarea、预览区、Form Item | 支持编辑、预览、保存、失败重试 | Markdown 编辑器完整交互待复核 |
| Job List | 岗位管理 | Table、Filter Dropdown、Pagination、操作列 | 新增岗位为主操作，匹配分析从操作列或详情进入 | 无 |
| Job Detail / Matching Analysis | 岗位详情与匹配分析 | Drawer、Accordion、Card、Score 展示 | 匹配分析为岗位详情折叠模块 | 匹配分析完整字段和解释结构待复核 |
| Job Manual Input | 岗位手动录入 | Drawer、Form Item、Input、Textarea | 只承接手动录入，不设计解析生成 | 岗位录入完整字段待复核 |
| Interview List | 模拟面试列表 | Table、Pagination、Filter Dropdown、操作列 | 支持查看详情、查看报告、发起模式 | 无 |
| Interview Detail Drawer | 模拟面试详情 | Drawer、Accordion、Card | 展示进展、关联输入和报告入口 | 无 |
| Interview Report Drawer | 面试报告详情 | Drawer、Accordion、Card、Copy Button | 支持内容复制，不支持文件导出 | 报告详情字段待复核 |
| Interview Launch Modal | 发起模拟面试 | Modal、Form Item、Select、Candidate Resume Table | 选择模式、岗位、简历、薄弱项后发起 | 候选选择单选 / 多选待复核 |
| Polishing Workbench | 打磨模式工作台 | App Shell、Progress Tree、Chat、Feedback Card、Suggestion Bar | 同题多轮回答、反馈、继续打磨、重新回答、切换节点、生成下一题 | 无 |
| Pressure Interview Workbench | 压力面模式工作台 | App Shell、Progress Tree、Chat、状态栏、输入区 | 真实节奏连续问答，动作不得默认继承打磨 | 压力面工作台语义：CONFLICT 已解除，M3 复核项；不代表 AIFI-UI-001 或 F3 已完成 |
| Review List | 面试复盘列表 | Table、Filter Dropdown、Pagination、操作列 | 区分模拟面试复盘和真实面试复盘 | 无 |
| Review Report Drawer | 复盘报告详情 | Drawer、Accordion、Card、Copy Button | 展示复盘结论、薄弱项、训练建议、沉淀入口 | 报告详情字段和沉淀链路待复核 |
| Review Create Modal | 新增复盘 | Modal、Form Item、Select | 选择复盘类型，进入对应表单 | 复盘表单字段和沉淀链路待复核 |
| Asset Library | 资产库 | Table、Drawer、Accordion | 展示资产来源、类型、正文、适用场景 | 无 |
| Weakness List | 薄弱项列表 | Table、Checkbox、Filter Dropdown、Pagination | 支持多选发起打磨 | 无 |
| Weakness Detail Drawer | 薄弱项详情 | Drawer、Accordion、Card | 展示来源证据、严重度、训练建议和关联对象 | 无 |
| Candidate Resume Table | 候选简历选择表格 | Table、Radio / Checkbox、Pagination | 用于发起模拟面试或岗位绑定 | 单选 / 多选待复核 |
| Content Confirmation Drawer | 内容沉淀确认 | Drawer、Checkbox、Form Item、Toast | 用户确认、编辑、取消目标、跳过、失败重试 | 复盘字段和沉淀链路待复核 |
| Low Confidence Review Drawer | 低置信度校对 | Drawer、Form Item、Error Retry | 展示待校对项、原因、用户修正和保存 | 低置信度阈值与规则待 F4 / F7 |

## 9. 状态体系

- 默认态：页面或组件可正常查看和操作。
- 空态：列表或内容区无数据，状态 Frame 替换内容区并提供创建、导入或返回入口。
- 加载态：表格、卡片、抽屉和报告使用 Loading Skeleton；不得遮挡已有主体。
- 错误态：错误说明替换内容区或显示在对应模块，必须提供重试、返回、稍后再试或联系说明。
- 成功反馈态：保存、复制、发起、沉淀成功后显示 Toast / Feedback，并在页面状态中体现结果。
- 错误反馈态：保存失败、复制失败、发起失败、沉淀失败时显示错误反馈和可恢复动作。
- 筛选展开态：Filter Dropdown 打开后提供搜索、单选 / 多选、应用、取消、重置、清空。
- 分页态：展示总数、当前页、页码、上一页 / 下一页、每页条数下拉和禁用态。
- 禁用态：不可用动作需可识别，并通过 tooltip、说明或内联文本解释原因。
- 保存中：提交按钮进入 loading，表单避免重复提交。
- 保存成功：显示成功反馈并更新本地页面状态。
- 保存失败：显示错误反馈、错误位置和重试入口。
- 低置信待校对态：展示待校对项和原因，不伪造低置信阈值。
- 内容沉淀待确认态：展示目标集合、可编辑内容、确认 / 跳过 / 取消目标和失败重试。

状态规则：

- 状态 Frame 不遮挡主体，必须替换表格区或内容区。
- Toast 不应作为唯一反馈，关键结果必须在页面或组件状态中可见。
- 错误反馈必须有可恢复动作或说明。
- 低置信度规则不得在 F3 中伪造阈值。

### 9.1 组件状态收敛矩阵

本矩阵用于补齐 F3 高保真组件状态表达。具体色值、字号、阴影参数和动效时长仍以 §14 token 状态为准；未被 active docs 证实的视觉数值不得写成最终事实。

| 组件 | 默认 | hover | focus | disabled | loading | error | warning | success |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Button | 展示主 / 次 / 危险层级和明确文案 | 背景或边框强化，不能只依赖 tooltip | 使用 `border.focus` 或焦点环语义 | 使用 `state.disabled`，必须说明原因 | 保留按钮宽度，显示 loading，禁止重复提交 | 用于提交失败后的错误反馈入口，不替代表单错误 | 用于风险确认或资料不足提醒 | 用于保存 / 复制 / 沉淀成功后的可见结果 |
| Icon Button | 图标可识别，操作列内对齐 | 显示 tooltip，图标背景轻微强化 | 可键盘聚焦并有可见焦点态 | 置灰并说明不可用原因 | 可显示小型 loading 或禁用重复点击 | 删除、解绑、复制失败等保留重试或确认 | 危险前置提醒或不可逆操作提示 | 操作成功后通过 Toast + 页面状态反馈 |
| Input | 展示 label、占位、当前值和帮助文本 | 边框轻微强化 | 焦点边框 / 焦点环可见 | 值可读但不可编辑 | 保存中避免重复输入或显示保存状态 | 错误信息靠近字段，字段自动定位 | 用于非阻塞风险或待补充提醒 | 保存成功后字段状态与页面状态同步 |
| Textarea | 支持多行、长文本滚动或换行 | 边框轻微强化 | 焦点态可见 | 不可编辑但内容可读 | 保存中避免重复提交 | 错误靠近字段并保留输入 | 长度、证据不足或待确认提示 | 保存成功后保留内容并显示反馈 |
| Select | 展示当前选择或占位 | 触发区强化 | 焦点回到触发入口 | 禁用时说明缺少前置资料 | 选项加载中显示 loading | 必选未选或加载失败显示错误 | 选择可能影响后续输入时提示 | 选择成功后更新关联页面状态 |
| Checkbox / Radio | 展示未选、已选和 indeterminate | 选项背景或边框强化 | 焦点态可见 | 置灰并说明原因 | 批量操作时避免重复变更 | 必选目标缺失时显示错误 | 多目标沉淀风险提示 | 选择成功后更新目标集合 |
| Tag | 展示来源、状态、类型或严重度 | 可交互 tag 显示 hover | 可删除 / 可选择 tag 有焦点态 | 不可编辑 tag 置灰但可读 | 批量更新时显示处理中 | 错误 tag 需有解释 | 警告 tag 表示资料不足或待确认 | 成功 tag 表示已保存、已沉淀或已完成 |
| Card | 展示标题、摘要、状态和主动作 | 可点击卡片层级强化 | 卡片操作可聚焦 | 禁用主动作并说明原因 | 骨架或模块 loading | 模块级错误和重试 | 模块级风险提示 | 模块成功状态不只依赖 Toast |
| Table | 展示表头、行、操作列和分页 | 行 hover 不改变操作语义 | 行内操作可聚焦 | 禁用操作说明原因 | Skeleton 行替换内容 | 表格区错误 Frame + 重试 | 行级风险、低置信或资料不足提示 | 行状态更新可见 |
| Drawer | 标题、正文滚动区、固定底部操作 | 交互元素有 hover | 关闭、取消、保存可聚焦 | 不可用动作说明原因 | 底部提交 loading，正文保留上下文 | 字段错误自动展开并定位 | 未保存关闭、破坏性操作二次确认 | 保存成功后状态更新并可关闭 |
| Modal | 标题、正文、固定底部操作 | 按钮 hover 清晰 | 焦点锁定在弹窗内 | 禁用确认说明原因 | 确认按钮 loading | 错误留在弹窗内可恢复 | 取消 / 关闭的未保存确认 | 成功后进入目标页面或更新来源列表 |
| Toast / Feedback | 成功、错误、警告、信息四类 | 不作为主要 hover 容器 | 不抢占关键焦点 | 不用于表达禁用原因唯一来源 | 不表达长期 loading | 错误 Toast 必须配合页面可恢复状态 | 警告 Toast 必须配合上下文说明 | 成功 Toast 必须配合页面结果变化 |
| Empty State | 无数据、无权限、资料不足 | 主动作 hover 清晰 | 主动作可聚焦 | 不可用主动作说明原因 | 可切换为加载 Frame | 加载失败转错误 Frame | 资料不足说明缺失项 | 创建或导入成功后回到默认态 |
| Loading Skeleton | 替代表格行、卡片或抽屉正文 | 不需要 hover | 不承接焦点 | 不作为禁用说明 | 表达加载中 | 加载失败转错误态 | 长时间加载可提示风险 | 加载完成转默认或空态 |
| Error Retry | 错误说明、重试、返回或稍后再试 | 重试按钮 hover 清晰 | 重试和返回可聚焦 | 无可恢复动作时说明原因 | 重试中显示 loading | 保留错误信息和上下文 | 多次失败可升级为警告说明 | 重试成功转默认态 |
| Low Confidence Review | 展示待校对项、原因和修正入口 | 可编辑项 hover 清晰 | 待校对项和保存可聚焦 | 缺少资料时说明原因 | 保存中避免重复提交 | 保存失败保留修正内容 | 低置信原因不伪造阈值 | 保存成功更新校对状态 |
| Human Review / Content Confirmation | 展示目标集合、可编辑内容和确认动作 | 目标项 hover 清晰 | 目标、编辑、确认、跳过可聚焦 | 目标不可选时说明原因 | 保存中保留目标集合 | 沉淀失败保留输入并可重试 | 自动写入风险必须提示 | 确认成功后更新资产 / 薄弱项 / 训练建议入口状态 |

### 9.2 页面状态收敛矩阵

| 页面 / 场景 | 默认态 | 空 / 资料不足 | 加载 | 错误 / 重试 | 保存 / 复制 / 生成中 | 禁用 / 低置信 / 人工确认 | UX 继承边界 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 工作台 | 卡片摘要、最近记录、推荐下一步 | 无简历 / 无岗位 / 无记录时引导创建 | 卡片和列表 Skeleton | 模块错误可重试 | 不直接生成报告或复盘 | 资料不足入口禁用并说明原因 | 左侧导航固定，不新增独立项目入口 |
| 简历 | 列表、详情抽屉、Markdown 编辑入口 | 未创建简历引导创建 / 导入 | 表格和抽屉 loading | 保存失败保留 Markdown 输入 | 保存中、保存成功、保存失败 | 解析失败或低置信进入校对，不静默覆盖 | 项目经历只作为简历模块 |
| 岗位 / JD | 岗位列表、详情、手动录入 | 未创建岗位引导手动录入 | 表格和抽屉 loading | 保存或绑定失败可重试 | 手动录入保存中 / 成功 / 失败 | 岗位资料不足解释缺失字段 | 不提供外部解析生成岗位路径 |
| 岗位匹配分析 | 在岗位详情折叠模块中展示分数、匹配点、不匹配点、加强点 | 未绑定简历或资料不足时提示 | 分析生成中 | 生成失败可重试 | 生成中不得伪造结果 | 低置信只提示校对，不定义算法阈值 | 不作为独立一级页面 |
| 打磨模式 | 四区工作台、同题多轮、反馈卡片 | 缺少简历 / 岗位 / 输入时阻止发起 | 题目或反馈生成中 | 题目 / 点评失败可重试 | 回答提交中、反馈生成中 | 低置信点评进入校对或提示 | 与压力面动作边界分离 |
| 压力面模式 | 四区工作台、连续追问、暂停、结束生成报告 | 资料不足阻止发起 | 题目生成中 | 追问或报告生成失败可恢复 | 回答提交中、报告生成中 | 不提供继续打磨 / 重新回答默认动作 | 不继承打磨模式动作语义 |
| 进展树 | 展示专题、节点、状态、右键菜单和当前题 | 无节点或资料不足时提示 | 节点生成中 | 节点生成失败可重试 | 生成题目中 | 禁用节点需说明原因 | 进展树不是 UI 装饰 |
| 报告 | 报告详情抽屉、评分、失分点、建议、复制入口 | 未生成报告或资料不足时提示 | 报告生成中 | 生成或读取失败可重试 | 复制中 / 复制成功 / 复制失败 | 不展示精确通过概率 | 无独立报告一级入口；复制不是导出 |
| 复盘 | 列表、新增复盘弹窗、复盘报告详情 | 无复盘引导新增 | 列表和详情 loading | 保存失败保留输入 | 新增保存中 / 成功 / 失败 | 沉淀目标需人工确认 | 模拟复盘和真实复盘区分 |
| 资产库 | 资产列表、详情抽屉、来源和适用场景 | 无资产时引导从打磨 / 报告 / 复盘沉淀 | 列表和详情 loading | 读取或保存失败可重试 | 保存中 / 成功 / 失败 | 低质量或来源待确认提示 | 资产库独立于简历 |
| 薄弱项 | 列表、详情、多选发起打磨 | 无薄弱项或资料不足时提示 | 列表 loading | 发起打磨失败可重试 | 多选发起中 | 禁用项说明缺少岗位 / 简历 / 证据 | 可进入打磨，不成为独立项目流 |
| 内容沉淀确认 | 目标集合、内容编辑、确认、跳过 | 无可沉淀内容时返回来源 | 目标加载中 | 沉淀失败保留输入并可重试 | 保存中 / 成功 / 失败 | 必须人工确认目标，不静默写入 | 回流到资产、薄弱项、训练建议或后续输入 |

页面状态必须继续遵守 F2 导航约束：不得新增独立项目一级入口、独立报告一级入口或独立报告复制一级入口。

## 10. 表格与列表规范

- 表头：承载列名、筛选图标、排序图标；不常驻黑底说明块，不使用 F / S 字母。
- 数据行：行高、列宽、文本省略和 hover 状态统一；名称列可进入详情。
- 操作列：固定在右侧，默认只显示 Icon Button，hover 显示 tooltip。
- 筛选：由表头筛选入口打开下拉，字段值因页面而异。
- 排序：支持默认、升序、降序三态。
- 分页：展示总条数、当前页、页码、上一页 / 下一页图标和每页条数下拉。
- 每页条数下拉：基础态只显示当前值，例如“每页 20 条 ▾”；展开态展示可选项。
- 最大承载状态：行数和分页语义保持一致，不以稀疏数据伪装完整列表。
- 空列表：状态 Frame 替代表格区，提供新增或返回入口。
- 加载骨架：替代表格行，保持表头和分页结构稳定。
- 错误重试：替代表格区，提供重试按钮和错误说明。

### 表头筛选下拉扩展区

#### 用途

- 用于表格列级筛选。
- 支持不同字段的筛选条件配置。
- 适用于简历、岗位、模拟面试、复盘、资产库、薄弱项、候选简历等列表。

#### 基础结构

- 字段名。
- 搜索框。
- 候选项列表。
- 单选 / 多选控件。
- 操作区。
- 操作区必须包含：应用、取消、清空当前字段、重置所有筛选。

#### 交互规则

- 点击「应用」：提交当前筛选条件并关闭面板。
- 点击「取消」：放弃本次未应用变更并关闭面板。
- 点击「清空当前字段」：清除当前字段筛选值，不影响其他字段。
- 点击「重置所有筛选」：清除当前表格全部筛选条件。
- 按 Esc：等同取消，放弃未应用变更并关闭面板。
- 点击面板外部：没有未应用变更时可关闭；存在未应用变更时，按取消处理，不得静默应用。
- 切换字段：已应用条件保留；未应用条件不得静默生效。
- 未应用变更必须有可识别状态或行为边界。

#### 单选 / 多选规则

- 单选用于互斥状态，例如投递状态、任务状态、是否启用。
- 多选用于可并存标签，例如来源、能力标签、薄弱项类型。
- 字段采用单选还是多选由业务字段定义；未明确字段标记为待复核。
- F3 规范不伪造后端筛选字段枚举。

#### 状态

- 默认态。
- 展开态。
- 搜索中。
- 无匹配结果。
- 已选择。
- 未应用变更。
- 应用中。
- 应用失败。
- 禁用态。

#### 可访问性

- 筛选入口可通过键盘聚焦。
- Esc 可关闭面板。
- 操作按钮必须有明确文案。
- 选中状态不能只依赖颜色表达。
- 筛选状态需要可被文本或辅助技术识别。
- 面板关闭后焦点应回到触发入口。

#### 与 Figma 证据关系

- Figma node 114:5944 覆盖了字段名、搜索、选项、重置、应用等低保真结构。
- Figma node 114:5944 未展示「取消」操作。
- 本 F3 规范基于 active docs 与复核结论补齐「取消」和未应用变更处理。
- 不得声称 Figma node 114:5944 已完整覆盖筛选交互闭环。
- 设计系统采用本 F3 补齐后的筛选规范，不直接采用 Figma 原样。

特别记录：

- 114:5944 表头筛选下拉缺少取消操作，F3 规范层已补齐“应用 / 取消 / 清空当前字段 / 重置所有筛选”规则及未应用变更处理。
- 单选 / 多选筛选模式由业务字段定义；在 F4 明确前不得伪造后端筛选字段枚举。

## 11. 表单规范

| 表单 | 默认字段范围 | 规则 | 待复核 |
| --- | --- | --- | --- |
| 岗位手动录入表单 | 岗位名称、所属公司、部门、岗位职责、岗位要求、其他、投递状态展示 | 仅支持手动录入，不提供外部解析生成 | 114:1529 岗位录入字段不足 |
| 简历 Markdown 编辑表单 | Markdown 正文、预览、系统识别模块、绑定岗位模块 | 不强制拆成多表单模块 | 114:628 Markdown 编辑细节不足 |
| 发起模拟面试表单 | 模式、岗位、简历、薄弱项、资产增强输入 | 不默认创建独立页面 | 候选简历选择模式待复核 |
| 新增面试复盘表单 | 复盘类型、关联对象或真实面试信息入口 | 可进入模拟面试复盘或真实面试复盘 | 114:3703 / 114:3754 字段不足 |
| 新增模拟面试复盘表单 | 关联模拟面试报告、题目、回答、表现、点评、失分点、薄弱项、改进建议、可沉淀资产 | 字段不得写成已完整验证 | 114:3703 字段和沉淀链路不足 |
| 新增真实面试复盘表单 | 真实面试经历、题目、回答、表现、失分点、薄弱项、改进建议、可沉淀资产 | 字段不得写成已完整验证 | 114:3754 字段和沉淀链路不足 |
| 内容沉淀确认表单 | 目标集合、沉淀内容、目标级确认、编辑、跳过、失败重试 | 不自动静默写入 | 沉淀链路待复核 |
| 低置信度校对表单 | 待校对项、原因、用户修正、保存状态 | 不伪造阈值 | 低置信规则待 F4 / F7 |

表单字段不得写成已完整验证；所有字段完整性在 F3 / F4 / F7 对应阶段复核。

## 12. 模拟面试工作台规范

### 12.1 打磨模式

- 打磨模式可以承载同题多轮修改和反馈。
- 工作台使用当前模拟面试情况栏、进展树、聊天窗口和输入区四区布局。
- 反馈卡片至少表达总体点评、0-100 打分、失分点、参考回答、参考回答与失分点对应关系、考点解析、技术原理扩展和下一步建议。
- 下一步建议固定在输入框上方，可包含继续打磨当前题、重新回答当前题、切换进展树节点和生成下一题。
- 长反馈内容默认折叠，展开后聊天窗口内部滚动。

### 12.2 压力面模式

- 压力面模式用于更接近真实面试节奏的连续问答。
- 114:2930 曾存在压力面模式混入打磨语义的问题；最新复核显示打磨默认动作已移除。
- 压力面模式不得默认继承打磨模式动作。
- 压力面模式不得默认提供同题无限改写路径。
- 压力面默认动作不得包含：继续打磨当前题、重新回答当前题、开始打磨。
- 压力面主操作应围绕：连续追问、继续下一题、暂停面试、结束并生成报告、查看当前进展。
- 压力面模式可以进入默认压力面 UI 规范。
- 打磨模式和压力面模式仍必须在 M3 高保真评审中继续检查语义边界。

## 13. 报告与复盘规范

- 面试报告详情：展示 0-100 总体评分、分项评分、表现总结、主要失分点、建议、薄弱项、训练方向、可回流资产和可复制内容；114:2430 报告详情字段不足，字段不得写成完整验证。
- 复盘报告详情：展示复盘结论、题目、回答、表现、点评、失分原因、薄弱项、改进建议、训练建议和沉淀入口；字段待复核。
- 报告复制：使用页面内范围选择、复制按钮、复制中状态、复制成功 / 失败反馈；不得将报告复制写成文件导出。
- 复盘新增：区分新增模拟面试复盘和新增真实面试复盘。
- 模拟面试复盘：来源于系统内完成的打磨模式或压力面模式，114:3703 复盘字段和沉淀链路不足。
- 真实面试复盘：来源于用户录入或整理的外部真实面试经历，114:3754 复盘字段和沉淀链路不足。
- 内容沉淀：不得自动静默写入，用户必须能够确认、编辑、取消目标、跳过和失败后重试。
- 低置信校对：展示待校对项和原因，不伪造阈值。
- 不得展示精确通过概率；如未来出现通过倾向或风险提示，需等待 F4 / F7 决定可信度说明和风险边界。

## 14. 低保真 token 草案

本节为 DRAFT。Token 草案来自 F2 低保真输入，不等于 F3 最终高保真 token，不得声称来自完整 Figma 实证定稿。

全局边界：本节 token 仅为语义草案，不冻结具体色值、字号、间距、圆角、阴影、动效时长、断点或组件尺寸。未由 Figma 高保真稿、Prototype 和人工评审确认的具体值必须标记为 `UNKNOWN`，不得写成已验证事实。

| Token 类别 | DRAFT 草案 | 说明 |
| --- | --- | --- |
| 色彩语义 | `color.text.primary`、`color.text.secondary`、`color.bg.canvas`、`color.bg.surface`、`color.border.default`、`color.status.success`、`color.status.error`、`color.status.warning`、`color.status.info` | 仅定义语义，不冻结具体色值 |
| 字体层级 | `font.title`、`font.section`、`font.body`、`font.caption`、`font.mono` | 字号、行高和字重待高保真补齐 |
| 间距 | `space.4`、`space.8`、`space.12`、`space.16`、`space.24`、`space.32` | 低保真间距输入转为语义草案 |
| 圆角 | `radius.sm`、`radius.md`、`radius.lg`、`radius.full` | 用于按钮、输入框、卡片、抽屉、弹窗 |
| 边框 | `border.default`、`border.subtle`、`border.focus`、`border.error` | 视觉强弱待 F3 高保真确认 |
| 阴影 | `shadow.drawer`、`shadow.modal`、`shadow.popover`、`shadow.card-hover` | 低保真不证明最终阴影参数 |
| 状态语义 | `state.default`、`state.hover`、`state.active`、`state.disabled`、`state.loading`、`state.error` | 跨组件复用 |
| 表单语义 | `form.required`、`form.hint`、`form.error`、`form.saving`、`form.success` | 表单校验和保存状态 |
| 反馈语义 | `feedback.success`、`feedback.error`、`feedback.warning`、`feedback.info` | Toast 不作为唯一反馈 |
| 数据展示语义 | `data.score`、`data.risk`、`data.match`、`data.weakness`、`data.evidence` | 不定义精确概率或算法阈值 |

## 15. 响应式规则草案

- 桌面优先：MVP 设计默认服务桌面中后台工作台体验。
- 左侧导航：桌面固定展示；小屏策略待复核。
- 主内容最小宽度：表格和工作台应有最小可读宽度；低于最小宽度时优先横向滚动或切换简化布局。
- 表格横向溢出：保留名称列、状态列和操作列可见性，其他列可横向滚动或省略。
- 抽屉宽度：桌面约为主内容区 35% - 45%；小屏可转为全屏抽屉，需后续复核。
- 弹窗宽度：短表单使用中等宽度，长表单可使用大弹窗或抽屉；按钮区固定。
- 小屏策略：候选简历表格、模拟面试工作台、进展树和聊天区的小屏重排为 UNKNOWN。
- LATER 项：移动端完整适配、触屏手势、复杂响应式断点和多窗口协作不进入当前 F3 默认草案。

## 16. 可访问性初检

- 键盘可达：导航、表格操作、筛选、排序、抽屉、弹窗、表单和复制按钮必须可通过键盘访问。
- 焦点态：Button、Icon Button、Input、Select、Checkbox、Accordion、Modal、Drawer 关闭入口必须有可见焦点态。
- 表单错误提示：错误信息应靠近字段，不能只依赖 Toast。
- 对比度：高保真色彩必须满足文本、边框、禁用态和状态提示的可读性；具体数值待 F3 高保真确认。
- Toast 非唯一反馈：保存、复制、沉淀等关键结果必须在页面状态中同步体现。
- 表格表头语义：表头、筛选、排序和列含义必须清晰；图标需要 tooltip 或可访问名称。
- 抽屉关闭入口：顶部关闭、取消和未保存二次确认必须可达。
- 弹窗关闭入口：顶部关闭、取消、Esc 行为和未保存确认待高保真 / 前端复核。
- 禁用态说明：禁用按钮、置灰视频 / 音频入口和不可用动作需要说明原因。

## 17. 高保真 Prototype 验收标准

Prototype 用于 M3 高保真评审中的信息架构、页面跳转、关键状态和交互语义验证，不等于真实产品实现。Prototype 不定义真实 API、数据模型、Prompt、评分算法、权限策略或持久化行为，不得覆盖 `docs/01-product/PRD.md` 或 `docs/02-design/UX_SPEC.md`。

### 17.1 必须可点击的主链路

- 工作台首页 → 简历列表 → 简历详情抽屉。
- 工作台首页 → 岗位列表 → 岗位手动录入抽屉。
- 岗位列表 → 岗位详情 / 匹配分析。
- 模拟面试列表 → 发起模拟面试弹窗。
- 发起模拟面试 → 打磨模式工作台。
- 发起模拟面试 → 压力面模式工作台。
- 压力面模式 → 结束并生成报告。
- 报告详情 → 复制报告内容 → 成功 / 失败反馈。
- 面试复盘列表 → 新增复盘弹窗。
- 面试复盘列表 → 复盘报告详情抽屉。
- 新增真实面试复盘 → 保存 → 复盘报告详情。
- 薄弱项列表 → 多选发起打磨。
- 内容沉淀确认 → 保存 / 跳过 / 失败重试。
- 低置信校对 → 修改 / 保存。
- 表头筛选 → 应用 / 取消 / 重置。
- 分页 → 每页条数切换。
- 用户设置 → 打开 / 关闭。

### 17.2 必须独立制作的状态 Frame

- 列表页默认态、空态、加载态和错误态。
- 抽屉 / 弹窗打开态、保存中、保存成功和保存失败。
- 报告复制中、复制成功和复制失败。
- 内容沉淀待确认、保存中、保存失败和失败重试。
- 低置信待校对、修改中和保存状态。
- 表头筛选展开、未应用变更、应用后关闭和取消后关闭。
- 分页每页条数下拉展开和禁用态。
- 资料不足导致无法发起、无法保存或无法生成报告的状态。

### 17.3 可静态展示的内容

- 非关键列表行数据、卡片摘要、报告示例文本和复盘示例文本可以使用静态占位内容。
- 分项评分、匹配分析、薄弱项建议和训练建议可以展示静态样例，但不得声称算法、评分公式或阈值已确定。
- 管理入口、通知偏好、角色展示等非主链路内容可以静态展示。

### 17.4 可延后项

- 移动端完整 Prototype、小屏复杂重排和触屏手势。
- 后端真实错误码、接口 loading 时长和真实分页数据联动。
- API、数据模型、Prompt、评分公式、文件导出和权限策略。
- F4 / F7 才能确认的低置信阈值、内容沉淀规则和质量门禁。

## 18. 高保真页面制作清单

| 页面 / Frame | 类型 | 是否必须制作 | 必须状态 | Prototype 要求 | 风险 |
| --- | --- | --- | --- | --- | --- |
| 工作台 | 页面 | 是 | 默认、加载、错误、资料不足 | 可点击进入简历、岗位、模拟面试、复盘、薄弱项 | 示例数据不得写成真实统计 |
| 简历 | 页面 + 抽屉 | 是 | 列表默认、空态、加载、错误、详情抽屉 | 工作台进入列表，列表进入详情抽屉 | 114:628 Markdown 编辑细节仍为 UNKNOWN |
| 岗位 | 页面 + 抽屉 | 是 | 列表默认、空态、手动录入、详情 / 匹配分析 | 工作台进入列表，列表进入录入抽屉和详情 / 匹配分析 | 114:1215 / 114:1529 字段不足 |
| 模拟面试 | 页面 + 弹窗 | 是 | 列表默认、空态、发起弹窗、资料不足 | 列表打开发起弹窗并进入两种模式 | 候选简历单选 / 多选待复核 |
| 打磨模式 | 工作台 | 是 | 默认、反馈展开、保存中、错误、资料不足 | 发起模拟面试后进入打磨模式工作台 | 不得与压力面动作混淆 |
| 压力面模式 | 工作台 | 是 | 默认、连续问答、暂停、结束并生成报告、错误 | 发起模拟面试后进入压力面，结束进入报告 | 114:2930 仍需 M3 复核边界 |
| 报告 | 抽屉 / 详情 Frame | 是 | 默认、复制中、复制成功、复制失败、资料不足 | 报告详情触发复制并展示成功 / 失败反馈 | 不得设计文件导出完成态 |
| 复盘 | 页面 + 弹窗 + 抽屉 | 是 | 列表默认、新增弹窗、保存中、保存失败、详情抽屉 | 列表新增复盘，保存后进入复盘报告详情 | 114:3703 / 114:3754 字段不足 |
| 资产库 | 页面 + 抽屉 | 是 | 列表默认、空态、加载、错误、详情抽屉 | 可静态展示列表和详情抽屉 | 沉淀来源字段待复核 |
| 薄弱项 | 页面 + 抽屉 + 弹窗 | 是 | 列表默认、多选、发起打磨弹窗、禁用态 | 多选后发起打磨 | 资料不足时必须解释禁用原因 |
| 候选简历 | 表格 Frame | 是 | 默认、选择、空态、加载、错误 | 嵌入发起模拟面试或岗位绑定流程 | 单选 / 多选模式待复核 |
| 内容沉淀 | 抽屉 / 确认 Frame | 是 | 待确认、保存中、保存成功、保存失败、跳过 | 保存 / 跳过 / 失败重试可点击 | 沉淀目标和字段待 F4 / F7 |
| 低置信校对 | 抽屉 / 校对 Frame | 是 | 待校对、修改中、保存中、保存成功、保存失败 | 修改并保存可点击 | 不得伪造低置信阈值 |
| 用户设置 | 抽屉 / 弹层 | 是 | 打开、关闭、禁用项说明 | 全局用户区打开 / 关闭 | 管理台完整能力不进入 F3 默认规范 |

嵌入式 / 非独立页面覆盖说明：训练建议作为报告、复盘、薄弱项、资产库或工作台中的模块覆盖，不新增独立训练建议一级页面；进展树作为打磨模式与压力面模式工作台核心组件覆盖，不新增独立进展树一级页面；用户设置作为全局用户区触发的抽屉或弹层覆盖，不新增独立用户设置一级页面。除非 active docs 后续明确授权，不得在 F3 高保真清单中扩展这些一级页面。

## 19. 状态 Frame 制作清单

| 状态 | 必须出现的页面 / 组件 | 表达方式 | 是否阻塞 M3 | 备注 |
| --- | --- | --- | --- | --- |
| 默认态 | 全部页面、抽屉、弹窗、工作台 | 正常内容、主操作、次操作、当前位置 | 是 | 作为其他状态对照基线 |
| 空态 | 简历、岗位、模拟面试、复盘、资产库、薄弱项、候选简历 | 状态 Frame 替代表格区，提供创建或返回入口 | 是 | 不得用空白页面代替 |
| 加载态 | 表格、抽屉、报告、工作台 | Skeleton 或模块级加载 | 是 | 保留页面结构稳定 |
| 错误态 | 表格、抽屉、报告、工作台 | 错误说明 + 重试 / 返回 / 稍后再试 | 是 | 错误不得只用 Toast 表达 |
| 保存中 | 表单、抽屉、弹窗、内容沉淀、低置信校对 | 提交按钮 loading，避免重复提交 | 是 | 不定义真实接口耗时 |
| 保存成功 | 表单、内容沉淀、低置信校对、复盘新增 | 成功反馈 + 页面状态更新 | 是 | Toast 不能作为唯一反馈 |
| 保存失败 | 表单、内容沉淀、低置信校对、复盘新增 | 错误反馈、错误位置、重试入口 | 是 | 失败后保留用户输入 |
| 复制中 | 报告、复盘报告 | 复制按钮 loading 或状态文案 | 是 | 不等同文件导出 |
| 复制成功 | 报告、复盘报告 | 成功反馈 + 可见复制结果状态 | 是 | 不生成文件 |
| 复制失败 | 报告、复盘报告 | 失败反馈 + 重试入口 | 是 | 不隐藏复制按钮 |
| 低置信待校对 | 低置信校对抽屉 | 待校对项、原因、修正输入、保存动作 | 是 | 不伪造阈值 |
| 内容沉淀待确认 | 内容沉淀确认抽屉 | 目标集合、可编辑内容、确认 / 跳过 / 取消目标 | 是 | 不自动静默写入 |
| 内容沉淀失败 | 内容沉淀确认抽屉 | 失败原因、保留输入、重试和跳过 | 是 | 失败可恢复 |
| 筛选展开 | Filter Dropdown | 搜索、选项、应用、取消、清空、重置 | 是 | 114:5944 低保真缺口已在规范层补齐 |
| 未应用变更 | Filter Dropdown | 明确未应用状态，取消不生效，应用才提交 | 是 | 点击外部不得静默应用 |
| 禁用态 | Button、Icon Button、分页、发起入口、保存入口 | 置灰 + tooltip 或内联说明 | 是 | 必须说明禁用原因 |
| 资料不足 | 工作台、发起模拟面试、报告、复盘、薄弱项发起打磨 | 状态 Frame 或禁用说明解释缺少简历 / 岗位 / 输入 | 是 | 不得绕过 PRD / UX_SPEC 扩展需求 |

## 20. M3 设计评审清单

| 评审项 | 通过标准 | 证据 | 失败影响 |
| --- | --- | --- | --- |
| 文档身份 | 明确阶段 F3、任务 AIFI-UI-001、里程碑 M3、状态 DRAFT | 本文档 §1 | 文档不能作为 M3 评审输入 |
| PRD 边界 | 未修改、替代或覆盖 PRD；不新增产品范围 | 本文档 §1、§4、§17 | 需要回到产品需求窗口处理 |
| UX_SPEC 继承 | 继承 F2 页面、流程、状态和边界，不覆盖 UX_SPEC | 本文档 §4、§5、§17 | 高保真可能偏离低保真基线 |
| 页面覆盖 | 页面制作清单覆盖工作台、简历、岗位、模拟面试、报告、复盘、资产库、薄弱项等 | 本文档 §18 | 高保真页面缺页，阻塞 M3 |
| 组件复用 | 基础组件和业务组件可映射到页面，不重复造组件 | 本文档 §7、§8 | 前端交接成本升高 |
| 状态覆盖 | 必须状态 Frame 有独立制作清单并标记是否阻塞 M3 | 本文档 §19 | Prototype 无法验证异常路径 |
| Prototype 可点击 | 关键主链路均可点击，延后项已标记 | 本文档 §17 | Prototype 评审证据不足 |
| 交互语义 | 保存、复制、筛选、分页、抽屉、弹窗语义清晰 | 本文档 §9、§10、§17、§19 | 前端实现可能误解状态边界 |
| 打磨 / 压力面边界 | 压力面不继承打磨动作，114:2930 继续 M3 复核 | 本文档 §12、§23 | 模式语义混淆，阻塞 M3 |
| 报告复制 / 文件导出边界 | 支持复制，不提供 PDF / Markdown / Word / 批量导出 | 本文档 §3、§13、§17、§18 | 误把复制设计成文件导出 |
| UNKNOWN / WARN / CONFLICT 台账 | 台账具备影响范围、是否阻塞、关闭方式、关闭证据和归属阶段 | 本文档 §23 | 未关闭风险无法追踪 |
| 响应式 | 桌面优先，小屏 UNKNOWN / LATER 不写成已完成 | 本文档 §15 | 评审误判移动端完成度 |
| 可访问性 | 键盘、焦点、错误提示、对比度、tooltip 有初检要求 | 本文档 §16 | 前端交接缺少可访问性基线 |
| 前端交接 | 明确 UI / 交互 / 状态语义可交接，F4 内容不可推断 | 本文档 §21 | F6 实现可能越过 F4 技术设计 |
| 状态矩阵检查 | 组件状态和页面状态矩阵已覆盖关键默认、加载、错误、空态、禁用、保存、复制、校对和沉淀状态，不把 `UNKNOWN` 写成事实 | 本文档 §9.1、§9.2 | 状态覆盖不可追踪，阻塞 M3 评审输入 |
| Figma 证据检查 | Figma 高保真稿、Prototype、Page 名称、链接和人工确认状态作为 M3 主证据；缺失项标记 `UNKNOWN` | 本文档 §4、§17、§22 | M3 证据不足或误把草案当定稿 |
| OpenDesign 候选产物边界 | OpenDesign 仅可作为 `CANDIDATE / UNVERIFIED / NOT_ACTIVE_SOURCE` 探索材料，不替代 Figma Prototype 或 active docs | 本文档 §4、§22 | 候选稿被误用为 M3 单独通过证据 |
| 无文件导出检查 | 高保真不得出现 PDF、Markdown 文件、Word / docx、批量导出、文件下载或文件生成入口 | 本文档 §13、§18、§19 | 超出 MVP 范围并与 PRD 边界冲突 |
| 无精确通过概率检查 | 不展示精确通过概率，不冻结评分公式、权重、阈值、校准或算法细节 | 本文档 §13、§14、§23 | 将 F4 / F7 未定内容误写为 F3 事实 |
| 项目经历一级对象检查 | 项目经历只作为简历模块和进展树维度覆盖，不新增项目经历一级业务对象或一级入口 | 本文档 §5、§12、§18 | 偏离 PRD 对一级对象的边界 |
| 岗位 / JD 手动录入边界 | 岗位 / JD 只覆盖手动录入，不设计外部文件解析、剪贴板批量解析或自动生成岗位路径 | 本文档 §11、§18 | 扩展到 PRD 未授权的自动解析能力 |

## 21. 前端实现交接清单

本文档只定义 UI / 交互 / 状态语义，不定义 API、数据模型、Prompt、算法、评分公式、真实错误码、权限策略或持久化机制。前端实现仍需等待 F4 技术设计确认后，才能把页面状态映射到 API contract、数据模型、Prompt 边界和错误语义。

| 交接项 | 前端可用信息 | 不能推断内容 | 依赖 |
| --- | --- | --- | --- |
| 页面结构 | App Shell、页面类型、抽屉、弹窗、工作台四区布局 | 路由策略、权限策略、真实数据来源 | F4 技术设计、API_SPEC |
| 组件清单 | Button、Drawer、Modal、Table、Filter Dropdown、Toast 等基础组件和业务组件 | 组件库技术选型、实现 API、状态管理方案 | F4 技术设计、前端实现任务 |
| 状态语义 | 默认、空、加载、错误、保存、复制、低置信、内容沉淀等状态 | 后端错误码、重试策略、接口时序 | F4 API / 数据设计 |
| Prototype 主链路 | 必须可点击的高保真评审路径 | 真实导航实现、浏览器路径、鉴权跳转 | F4 技术设计、F6 前端开发 |
| 表格与筛选 | 表头筛选、排序、分页、每页条数和未应用变更语义 | 后端筛选字段枚举、查询参数格式 | F4 API_SPEC / DATA_MODEL |
| 报告复制 | 复制按钮、复制中、复制成功、复制失败反馈 | 文件导出、批量导出、真实 Clipboard 兼容策略 | F4 技术设计、F7 测试 |
| 打磨 / 压力面边界 | 两种模式的视觉和动作语义边界 | LLM 编排、题目生成、评分算法、追问策略 | F4 PROMPT_SPEC / TECH_DESIGN |
| 内容沉淀 | 确认、编辑、取消目标、跳过、失败重试语义 | 沉淀规则、资产写入模型、冲突合并策略 | F4 DATA_MODEL / API_SPEC |
| 低置信校对 | 待校对项、原因、用户修正和保存状态 | 低置信阈值、算法判断、模型置信来源 | F4 / F7 设计与测试 |
| 响应式与可访问性 | 桌面优先、小屏待复核、键盘和焦点基线 | 具体断点、前端技术实现、自动化测试覆盖 | F4 技术设计、F6 / F7 |

## 22. 高保真交付物索引

本节只建立高保真交付物索引结构。Figma 高保真链接未知时必须写 `UNKNOWN`，不得伪造 Figma 链接，不得声称高保真已完成。状态只能使用 `NOT_STARTED` / `IN_PROGRESS` / `REVIEW_READY` / `APPROVED` / `UNKNOWN`。

| 交付物 | 类型 | 对应页面 / 组件 | Figma 链接 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| F3 高保真页面总览 | 页面索引 | 全部页面 / Frame | UNKNOWN | NOT_STARTED | 当前仅有文档索引结构 |
| 工作台高保真 | Page / Frame | 工作台 | UNKNOWN | NOT_STARTED | 需覆盖默认、加载、错误、资料不足 |
| 简历高保真 | Page / Drawer | 简历列表、详情抽屉、Markdown 编辑 | UNKNOWN | NOT_STARTED | 114:628 待复核 |
| 岗位高保真 | Page / Drawer | 岗位列表、岗位详情 / 匹配分析、手动录入 | UNKNOWN | NOT_STARTED | 114:1215 / 114:1529 待复核 |
| 模拟面试高保真 | Page / Modal | 模拟面试列表、发起弹窗 | UNKNOWN | NOT_STARTED | 候选简历选择待复核 |
| 打磨模式高保真 | Workbench | 打磨模式工作台 | UNKNOWN | NOT_STARTED | 需与压力面区分 |
| 压力面模式高保真 | Workbench | 压力面模式工作台 | UNKNOWN | NOT_STARTED | 114:2930 需 M3 复核 |
| 报告高保真 | Drawer / Detail Frame | 面试报告、复制反馈 | UNKNOWN | NOT_STARTED | 114:2430 待复核；不设计文件导出 |
| 复盘高保真 | Page / Modal / Drawer | 复盘列表、新增复盘、复盘报告详情 | UNKNOWN | NOT_STARTED | 114:3703 / 114:3754 待复核 |
| 资产库高保真 | Page / Drawer | 资产库列表、资产详情 | UNKNOWN | NOT_STARTED | 沉淀来源字段待复核 |
| 薄弱项高保真 | Page / Drawer / Modal | 薄弱项列表、详情、多选发起打磨 | UNKNOWN | NOT_STARTED | 资料不足禁用态需独立 Frame |
| 候选简历高保真 | Component Frame | 候选简历选择表格 | UNKNOWN | NOT_STARTED | 单选 / 多选待复核 |
| 内容沉淀高保真 | Drawer / State Frame | 内容沉淀确认、保存 / 跳过 / 失败重试 | UNKNOWN | NOT_STARTED | 不自动静默写入 |
| 低置信校对高保真 | Drawer / State Frame | 低置信校对、修改、保存 | UNKNOWN | NOT_STARTED | 不伪造阈值 |
| 用户设置高保真 | Drawer / Popover | 用户设置打开 / 关闭 | UNKNOWN | NOT_STARTED | 管理台完整能力不进入默认规范 |
| 组件状态高保真 | Component Set | Button、Table、Filter Dropdown、Drawer、Modal 等 | UNKNOWN | NOT_STARTED | 需覆盖 §19 状态 |
| Prototype 主链路 | Prototype | §17 必须可点击链路 | UNKNOWN | NOT_STARTED | 不等于真实产品实现 |

## 23. WARN / UNKNOWN / CONFLICT 台账关闭格式

WARN / UNKNOWN / CONFLICT 台账必须可关闭、可追踪。关闭不代表 F3 或 AIFI-UI-001 完成；仅表示对应风险项已有关闭证据，且未把 F4 / F7 内容提前写成 F3 完成事实。

| 项 | 类型 | 影响范围 | 是否阻塞 M3 | 关闭方式 | 关闭证据 | 归属阶段 |
| --- | --- | --- | --- | --- | --- | --- |
| 114:628 | UNKNOWN | 简历 Markdown 编辑抽屉、预览、保存、失败重试 | 是 | 高保真补齐编辑 / 预览 / 保存 / 失败状态，并由人工评审确认 | UNKNOWN | F3 |
| 114:1215 | UNKNOWN | 岗位详情、匹配分析模块、解释结构 | 是 | 高保真补齐匹配分析展示结构；算法和字段边界交给 F4 | UNKNOWN | F3 / F4 |
| 114:1529 | UNKNOWN | 岗位手动录入抽屉、字段完整性、保存状态 | 是 | 高保真补齐表单状态；字段完整性不得越过 PRD / UX_SPEC | UNKNOWN | F3 |
| 114:2430 | UNKNOWN | 报告详情、评分、失分点、建议、复制入口 | 是 | 高保真补齐报告详情 Frame 和复制状态；文件导出保持不进入范围 | UNKNOWN | F3 |
| 114:2930 | CONFLICT 已解除 / M3 复核项 | 压力面模式与打磨模式动作语义边界 | 是 | M3 评审逐项核对压力面无继续打磨、重新回答、开始打磨等动作 | UNKNOWN | F3 |
| 114:3703 | UNKNOWN | 新增模拟面试复盘、复盘字段、沉淀链路 | 是 | 高保真补齐新增复盘弹窗、保存失败和复盘报告详情路径 | UNKNOWN | F3 / F4 |
| 114:3754 | UNKNOWN | 新增真实面试复盘、真实面试字段、沉淀链路 | 是 | 高保真补齐真实面试复盘保存到详情链路；字段边界待 F4 | UNKNOWN | F3 / F4 |
| 114:5944 | WARN / CONFLICT 规范层已关闭 | 表头筛选缺少取消操作、未应用变更语义 | 否 | 以本文档 §10 和 §19 作为 F3 规范层关闭；Figma 源稿是否修订由人工决定 | 本文档已补齐应用 / 取消 / 清空 / 重置 / 未应用变更语义 | F3 |
| node.type / Page 归属 | UNKNOWN | Figma Page 归属、node.type 证据完整性 | 是 | M3 前补充 metadata 或人工确认 Page / Frame 归属 | UNKNOWN | F3 |

## 24. 验收标准

- 文件存在。
- 覆盖页面类型。
- 覆盖组件类型。
- 覆盖状态类型。
- 覆盖交互模式。
- 覆盖低保真 token 草案。
- 覆盖 WARN / UNKNOWN / CONFLICT 台账。
- 明确 Figma 证据边界。
- 未把 CONFLICT 写成默认规范。
- 未把 UNKNOWN 写成已验证事实。
- 页面制作清单应完成。
- Prototype 验收标准应完成。
- 状态 Frame 制作清单应完成。
- M3 设计评审清单应完成。
- 前端实现交接清单应完成。
- 高保真交付物索引存在。
- WARN / UNKNOWN / CONFLICT 台账具备关闭状态。
- 未把 F4 内容写成 F3 已完成。
- 未把 Figma 低保真证据写成高保真完成事实。
- 不将 AIFI-UI-001 或 F3 标记 DONE。
