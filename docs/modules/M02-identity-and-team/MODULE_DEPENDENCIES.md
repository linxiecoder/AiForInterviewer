# M02 鉴权、团队与成员 - 模块依赖

## 1. 上游依赖

| 来源 | 依赖内容 | 对 M02 的作用 | 当前判断 |
| --- | --- | --- | --- |
| `AGENTS.md` | 文档语言、索引与 Markdown 安全读写规则 | 约束本轮文档编辑与回写边界 | 已满足 |
| `docs/DOC_GOVERNANCE.md` | 模块 Codex 责任边界、成熟度与收口规则 | 约束本轮只修改 `M02` 模块级文档 | 已满足 |
| `PLAN_LATEST.md` | `M02` 模块目标与模块顺序 | 确认本模块是第一轮优先模块 | 已满足 |
| `TECHNICAL_STANDARDS.md` | `Next.js + FastAPI + PostgreSQL`、开发态认证适配层口径、治理与可观测性要求 | 提供技术边界与默认口径 | 已满足 |
| `DESIGN_DECISIONS.md` | 全局分层、单次实施单位、技术路线 | 约束本模块输出给下游的形式 | 已满足 |
| `OPEN_QUESTIONS.md` | `OQ-004`、`OQ-005` 的默认冻结口径 | 作为本轮 `M02` 收敛到 `L5` 候选的输入 | 已满足 |
| `M01` 模块文档 | 配置读取、App Shell、i18n、列表原语、日志与测试基线 | 为登录页、成员列表与鉴权测试提供前置约束 | 够支撑评审，尚不足以支撑实施 |

## 2. M01 对 M02 的直接输入

### 2.1 技术与配置输入

- monorepo 结构、`apps/web` / `apps/api` / `infra`
- `.env` 读取基线
- FastAPI 路由挂载方式
- Next.js App Shell 与 i18n 入口约束

### 2.2 页面与测试输入

- `M01` 的工作台壳层决定 `/(dashboard)` 下受保护页面的承接方式。
- `M01` 的列表原语与服务端分页思路影响 `/members` 的列表 envelope 设计。
- `M01` 的测试 / 日志基线要求 `M02` 从第一轮就建立鉴权矩阵测试文件。
- `M02` 已冻结 `/members` 为团队内基础成员目录，因此列表原语只需承接 `id`、`displayName`、`role`、`status` 四字段基线。

## 3. 模块内依赖链

| 子任务 | 主要产出 | 依赖前置 | 当前判断 |
| --- | --- | --- | --- |
| `ST02_01` 鉴权机制与会话边界 | 登录、当前用户、退出边界；固定 token 解析规则 | `M01` 配置 / App Shell 基线，`OQ-004` | 最先推进 |
| `ST02_02` 团队、用户与成员目录 | `teams` / `users` 对象、成员目录读取契约 | `ST02_01` 的当前用户上下文，`M01` 列表原语 | 次序排在 `ST02_01` 之后 |
| `ST02_03` 授权矩阵与管理员/成员边界 | 页面 / API 权限矩阵、`401 / 403 / 404` 测试骨架 | `ST02_01` 与 `ST02_02`，以及已冻结的 admin 管理台分工口径 | 依赖最多，最后推进 |

## 4. 下游消费者

### 4.1 直接消费 M02 输出的模块

- `M03-M09`
  - 复用 `current_user / team_id / role`
  - 复用团队过滤与软删除过滤原则
- `M10`
  - 复用 `admin` 路由组授权判断
  - 需要与 `M02` 对齐成员管理页 / API 的职责拆分

### 4.2 共享契约输出

- 当前用户上下文
- 团队作用域过滤规则
- 管理员 / 成员路由组基线
- 鉴权异常语义：
  - `401`
  - `403`
  - `404`
  - 软删除过滤

## 5. 当前阻塞与风险

- 当前模块级关键契约已冻结，但是否进入下游子任务设计仍取决于模块评审通过与文档成熟度达标。

## 6. 当前 readiness 判断

- `M02` 模块文档已具备“进入评审”的上游依赖。
- `M02` 模块文档已清空模块级待确认问题，但尚未自动等同于“可直接开启下一轮子任务设计”。
- 若要进入下一轮子任务设计，建议先完成：
  1. 复核 `M01` 列表 / App Shell 基线是否足以承接 `/members` 页面约束
  2. 向总控同步 `MQ-202`、`MQ-203`、`MQ-204` 已确认的模块口径
