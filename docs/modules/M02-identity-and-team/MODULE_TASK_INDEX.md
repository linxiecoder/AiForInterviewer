# M02 鉴权、团队与成员 - 模块任务索引

## 1. 模块任务总表

| Subtask ID | 子任务名称 | 本轮定位 | 关键输入 | 关键输出 | 前置依赖 | 当前阻塞 | 是否建议下一轮启动子任务设计 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ST02_01` | 鉴权机制与会话边界 | 首个拆分单元，负责开发态认证适配层与当前用户解析 | `MODULE_REQUIREMENTS`、`MODULE_API_DESIGN`、`MODULE_LOGIC_DESIGN`、`OQ-004` | `/auth/login`、`/auth/me`、`/auth/logout`、`CurrentUserContext` | `M01` 配置 / App Shell 基线 | 无模块级待确认问题 | 否 |
| `ST02_02` | 团队、用户与成员目录 | 承接 `teams` / `users` 与 `/members` 读取边界 | `MODULE_REQUIREMENTS`、`MODULE_SCHEMA_DESIGN`、`MODULE_API_DESIGN`、`M01` 列表原语基线 | `teams` / `users` 设计细化、成员目录列表 / 详情子任务设计 | `ST02_01` 的当前用户上下文 | 无模块级待确认问题 | 否 |
| `ST02_03` | 授权矩阵与管理员/成员边界 | 负责权限矩阵、管理台路由基线和鉴权测试骨架 | `MODULE_DESIGN`、`MODULE_API_DESIGN`、`MODULE_LOGIC_DESIGN`、`OQ-005` | 页面 / API 权限矩阵、`401 / 403 / 404` 测试设计 | `ST02_01`、`ST02_02`、已冻结的 admin 管理台分工口径 | 无模块级待确认问题 | 否 |

## 2. 建议推进顺序

1. `ST02_01`
2. `ST02_02`
3. `ST02_03`

## 3. 当前判断

- 三个子任务的模块级上游已经从“骨架”提升到“可作为下游输入候选”。
- 但按照文档治理规则，子任务设计启动前，模块需求 / 设计 / API / schema / logic 至少应达到 `L5`。
- 当前 `M02` 已具备 `L5` 候选条件，但在总控未正式回写成熟度前，本轮收口判断仍为：
  - `ST02_01`：待总控确认后再启动
  - `ST02_02`：待总控确认后再启动
  - `ST02_03`：待总控确认后再启动

## 4. 下一轮开启子任务设计前必须满足

- 模块评审通过，并确认 `MODULE_REQUIREMENTS / DESIGN / API / SCHEMA / LOGIC` 已达到治理要求的下游输入成熟度。

## 5. 使用规则

- 在总控未正式回写 `L5` 前，不要直接补写子任务双文档来掩盖上游缺口。
- 若下一轮进入子任务设计，优先从 `ST02_01` 开始，避免先在目录页或权限矩阵里重复发明当前用户上下文契约。
- `MQ-202` 已在模块内冻结：`/members` 为团队内基础成员目录，字段集固定为 `id`、`displayName`、`role`、`status`，跨团队详情访问返回 `403`。
- `MQ-203` 已在模块内按方案 B 冻结：`M02` 只负责 admin 鉴权与最小契约，完整成员管理实现后移到后续治理模块。
- `MQ-204` 已在模块内按方案 A 冻结：`teams.status` / `users.status` 只冻结 `active`，软删除继续由 `deleted_at` 表达。
