---
title: R1 Penpot 低保真页面规格
type: design
permalink: ai-for-interviewer/docs/design/workbench-mvp/r1-penpot-lowfi-spec
---

# R1 Penpot 低保真页面规格

## 1. 文档定位

本文档用于 R1 acceptance closeout 前的页面设计校准，目标是把现有 R1 需求、设计、前端实现、E2E 断言和用户反馈整理为 Penpot 低保真页面输入。

本文档只定义低保真页面规格、Penpot frame 规划、Penpot MCP 后续执行方式和后续窗口计划。

本文档不修改业务代码，不修改前端代码，不修改测试，不修改数据库，不修改 `docs/governance/**`，不修改 `DOC_STATE.yaml`，不生成 implementation packet，不进入 R1 closeout，不声明任何任务 implementation-ready。

## 2. 输入事实

本规格基于以下现有事实源整理：

| 来源 | 已确认事实 |
| --- | --- |
| `docs/requirements/workbench-mvp/**` | Workbench MVP 目标是文本优先的模拟面试工作台，覆盖岗位、简历、材料、问题、回答、评分、复盘、导出和历史回看闭环。 |
| `docs/design/workbench-mvp/information-architecture.md` | 一级导航包括工作台、模拟面试、岗位、简历、知识库、复盘 / 评分、账号 / 权限；核心路径调整为模拟面试列表、发起模拟面试、模拟面试页、评分 / 复盘详情。 |
| `docs/design/workbench-mvp/object-model-rag-multiround-backend.md` | 参考资料、面试过程记录、评分、复盘、导出和弱项训练需要可追踪引用、可恢复状态和资料不足提示。 |
| `docs/design/workbench-mvp/scoring-review-export-dod.md` | 评分必须支持综合分、多维评分、评分理由、证据、低置信度、建议、弱项和 Markdown 导出。 |
| `docs/development/r1-trusted-trace-ui-compliance.md` | 根路径 `/` 已切到 R1 工作台首页；旧 W10 mock 只保留在 `/legacy-mock` 或 `/mock`；Ant Design 当前是 R1 试点。 |
| `docs/development/local-startup.md` | 当前前端 route 覆盖 `/`、`/interviews`、`/interviews/:sessionId`、`/reviews`、`/jobs`、`/resumes`、`/interviews/new`，E2E 通过 mock API 验证。 |
| `docs/development/database.md` | 前端只展示安全摘要和引用，不展示完整 prompt、raw LLM response、secret、对象存储真实路径或隐藏 resource id。 |
| `apps/web/src/**` | 当前页面已有工作台首页、模拟面试记录列表、复盘列表、详情页和若干入口页，但布局仍由代码直接推断，未经过 Penpot 低保真校准。 |
| `apps/web/e2e/trusted-trace.spec.ts` | 浏览器验收已覆盖根路径工作台首页、旧 mock 隔离、模拟面试记录到详情、复盘入口、可信详情、早期记录空态、敏感字段不展示。 |

用户反馈已经确认：后续不能继续完全依赖 Codex 基于文字和现有代码自行推断布局，必须引入 Penpot 低保真页面稿作为前端实现依据。

## 3. 全局页面原则

### 3.1 布局优先级

后续前端实现的布局优先级固定为：

1. 已经由用户确认的 Penpot frame。
2. 本文档 low-fi spec。
3. 现有需求和设计文档。
4. 当前前端代码和 E2E 断言。

现有代码只能作为能力事实和字段事实，不能作为最终布局依据。

### 3.2 文案分层规则

低保真页面稿必须区分三层语言，避免把工程、测试或状态机术语直接放到用户可见界面上。

| 语言层 | 使用位置 | 表达要求 |
| --- | --- | --- |
| 用户可见语言 | Penpot frame、页面标题、区块标题、按钮、状态提示、前端主文案 | 使用业务语言，例如“工作台概览”“最近模拟面试”“复盘结果”“参考资料”“证据不足”“评分理由”“改进建议”“薄弱项”“可重新导出”“暂无记录”“加载失败”“部分资料不可用”“数据已保存，可随时回看”。 |
| 产品说明语言 | 本文档解释段落、页面目标、低保真意图说明 | 可以说明“该区域用于表达数据可恢复能力”“该区域用于展示复盘证据链”“该状态用于提示资料不足导致可信度下降”。 |
| 工程验收语言 | Codex 实现注意事项、验收规则、禁止项、开发文档引用 | 可以保留 `PostgreSQL`、`traceability`、`E2E`、`RAG citation`、`degraded`、`failed`、`empty`、`legacy mock`、`hidden resource id` 等内部术语，但不得作为页面主文案或 frame 画布正文。 |

页面稿中出现技术状态时，必须先转换为用户能理解的业务状态；工程字段名只允许出现在实现说明、验收说明或代码相关引用中。

### 3.3 全局 App Shell / 信息架构

R1 所有主页面必须采用统一 App Shell。App Shell 是后续 Penpot frame 和前端实现的最高优先级布局约束，优先级高于当前前端代码中已有的顶部导航形态。

统一 App Shell 必须包含：

| 区域 | 低保真要求 |
| --- | --- |
| 左侧全局导航 | 固定承载 R1 主要业务栏目，导航选中态随右侧详情区路由变化。 |
| 右侧页面详情区 | 承载当前页面标题、主内容、状态区、主要操作和空态 / 失败态 / 降级态。 |
| 右上角用户登录信息区 | 在右侧详情区顶部或全局顶栏右侧展示用户头像、用户名称、个人配置、偏好设置和退出登录入口。 |

左侧导航只承载主要业务栏目，不放用户个人配置。用户个人配置必须放在右上角头像下拉中。旧 mock 入口不得出现在主导航，只允许作为开发辅助入口或隐藏入口。

左侧全局导航推荐 IA：

| 一级栏目 | 二级入口 |
| --- | --- |
| 工作台总览 | 工作台首页 |
| 简历和岗位管理 | 岗位管理、简历管理 |
| 模拟面试 | 模拟面试列表、发起模拟面试 |
| 复盘 | 复盘列表、评分 / 复盘详情 |
| 知识库 | 我的资料、公共知识库、资料处理状态 |

“发起模拟面试”可以作为模拟面试列表页的主按钮，也可以作为二级入口。左侧导航空间有限时，优先展示一级栏目，二级入口在页面内体现。

右上角用户登录信息区至少包含：

- 用户头像。
- 用户名称或账号标识。
- 下拉菜单。
- 个人配置入口。
- 偏好设置入口。
- 退出登录入口。

用户下拉菜单建议包含：

- 个人配置。
- 偏好设置。
- 安全与隐私。
- 退出登录。

右侧详情区按页面类型切换：

| 页面类型 | 右侧详情区内容 |
| --- | --- |
| 工作台首页 | 工作台概览、快捷入口、最近记录、复盘能力摘要。 |
| 简历和岗位管理 | 列表、创建入口、状态、空态。 |
| 模拟面试 | 模拟面试列表、发起流程、模拟面试页。 |
| 复盘 | 复盘列表、评分详情、证据链、建议、薄弱项。 |
| 知识库 | 资料列表、资料处理状态、参考资料不可用和证据不足状态。 |

### 3.4 工作台页面共同结构

所有 R1 页面低保真稿应包含：

- 统一 App Shell，保持左侧全局导航、右侧页面详情区和右上角用户登录信息区稳定。
- 当前页面标题、业务状态和关键能力标签。
- 主要内容区，优先展示当前页面最需要完成的任务，而不是大面积说明文案。
- 状态区，统一表达加载中、暂无记录、部分资料不可用、加载失败、可重新尝试、部分资料不可见。
- 安全信息边界，明确不展示敏感字段。
- 可进入下一步的主操作，避免只有静态空态。

### 3.5 状态表达

低保真稿必须显式预留以下状态：

| 内部状态 | 页面表达 |
| --- | --- |
| loading | 加载中，使用骨架或轻量提示，不使用空白屏。 |
| empty | 暂无记录 / 暂无复盘，并提供下一步动作。 |
| degraded | 部分资料不可用，结果可信度降低。 |
| failed | 加载失败 / 生成失败，并提供恢复动作。 |
| retryable | 可重新尝试。 |
| permission_filtered / permission-hidden | 部分资料不可见，不泄露对象是否真实存在。 |
| rag_unavailable | 参考资料暂不可用。 |
| index_pending | 资料处理中。 |
| index_failed | 资料处理失败。 |
| no_result | 暂无匹配资料。 |

页面上不得直接出现 `degraded`、`failed`、`empty`、`retryable`、`permission_filtered`、`rag_unavailable`、`index_pending`、`index_failed` 或 `no_result` 等英文技术状态。

### 3.6 安全展示边界

所有页面禁止展示：

- full prompt。
- raw LLM response。
- secret、token、password、provider key。
- 完整数据库连接串。
- object storage path。
- hidden resource id。
- embedding vector。
- provider 原始载荷。

页面可以展示安全摘要、业务状态、综合评分、评分理由、参考来源、证据不足提示、导出版本摘要和用户可理解的失败原因。

## 4. 页面清单

| 页面 | Route | Penpot frame | R1 目标 |
| --- | --- | --- | --- |
| 工作台首页 | `/` | `R1 Workbench Home` | 成为工作台默认首页，汇总主链路入口、最近记录和复盘能力。 |
| 岗位管理页 | `/jobs` | `R1 Jobs List` | 展示岗位、投递公司、投递进展、关联简历、版本和发起模拟入口。 |
| 岗位详情与版本页 | 后续 route 建议 `/jobs/:jobId` | `R1 Job Detail and Versions` | 展示岗位详情、当前版本、历史版本、关联简历和关联模拟面试。 |
| 简历管理页 | `/resumes` | `R1 Resumes List` | 展示简历、关联岗位、版本、解析状态和详情入口。 |
| 简历详情与匹配度页 | 后续 route 建议 `/resumes/:resumeId` | `R1 Resume Detail and Match Evaluation` | 展示简历详情、历史版本、关联岗位和简历与岗位匹配度评估。 |
| 模拟面试列表页 | `/interviews` | `R1 Simulation Interview List` | 展示模拟面试主业务列表、关联岗位、关联简历、模拟进度、模拟类型和进入复盘 / 面试页的操作。 |
| 发起模拟面试页 | `/interviews/new` | `R1 Start Simulation Interview` | 选择岗位、简历、模拟类型、主题、次主题和参考资料后进入模拟面试页。 |
| 模拟面试页 | 后续 route 建议 `/interviews/:sessionId/room` | `R1 Simulation Interview Room` | 承载多种模式的模拟面试窗口，包含上方概括、左侧进展树、右侧面试窗口和右下输入区。 |
| 评分 / 复盘详情页 | `/interviews/:sessionId` | `R1 Trusted Review Detail` | 展示可信评分、复盘、参考来源、证据不足、面试过程记录和导出状态。 |
| 复盘列表页 | `/reviews` | `R1 Reviews List` | 汇总最近复盘、关联模拟面试、关联岗位、关联简历、分数、状态、证据不足和详情入口。 |
| 知识库 | 后续 route 待实现，建议 `/knowledge` | `R1 Knowledge Base` | 为资料列表、公共知识库、资料处理状态和证据不足预留入口。 |
| 空态与降级态集合 | 多页面适用 | `R1 Empty and Degraded States` | 统一模拟面试、岗位、简历、复盘、资料不可用、证据不足、早期记录缺少过程信息、早期记录缺少评分等状态。 |

旧 W10 mock 不允许作为主界面；只能保留在 `/legacy-mock` 或 `/mock`。

## 5. 页面规格

### 5.1 工作台首页

页面目标：根路径 `/` 的工作台默认首页，帮助用户从一个清晰的工作台进入发起模拟、模拟面试列表、岗位、简历、复盘和可信详情。

必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| App Shell | 左侧全局导航选中“工作台总览”，右侧详情区显示首页内容，右上角显示用户头像、用户名称、个人配置、偏好设置和退出登录入口。 |
| 顶部区域 | 在右侧详情区顶部显示产品名、当前用户摘要、面试准备状态和工作台概览。 |
| 主导航 | 左侧导航展示工作台总览、简历和岗位管理、模拟面试、复盘、知识库；旧 mock 不进入主导航。 |
| 工作台概览 | 展示“数据已保存，可随时回看”“参考资料已关联”“面试过程记录”“综合评分”“复盘结果可查看”，并预留“部分资料不可用”“加载失败”状态位。 |
| 主操作入口 | 首要操作是“发起模拟面试”；次要操作是模拟面试列表、岗位管理、简历管理、复盘。所有入口必须是真实 route，不允许 `#anchor` 假入口。 |
| 最近模拟面试 | 展示模拟面试名称或编号、更新时间、当前状态、过程信息完整度、综合评分、复盘状态、导出状态、证据不足摘要和“查看复盘结果”。 |
| 复盘能力摘要 | 汇总面试过程记录、参考来源、证据不足、综合评分、评分理由、改进建议和 Markdown 导出。 |
| 岗位 / 简历 / 知识库入口 | 首页应显示三个准备对象的可用性摘要，并提供进入对应管理页或发起页的动作。 |
| 模拟面试 / 复盘入口 | 首页应可进入 `/interviews` 和 `/reviews`，并能从最近记录直接进 `/interviews/:sessionId`。 |
| 状态表达 | 首页必须展示最近记录加载失败、暂无记录、早期记录暂无完整过程信息、参考资料暂不可用、暂无匹配资料、导出失败时可重新尝试等低保真状态。 |

Codex 实现注意事项：

- 后续实现不得仅照抄当前 Ant Design card 堆叠；布局以 Penpot 首页 frame 为准。
- 首页可以保留 Ant Design 作为组件基础，但信息架构和视觉密度以 Penpot frame 为准。
- 首页只能展示安全摘要，不展示 prompt、raw response、secret 或私有存储路径。
- 根路径 `/` 必须呈现统一 App Shell，而不是只呈现单页顶部导航。
- 根路径不得出现旧 mock 主界面文案；旧 mock 只能保留在 `/legacy-mock` 或 `/mock`；Penpot 主工作台 frame 不得包含旧 mock 原型文案。

### 5.2 模拟面试列表页

页面目标：`/interviews` 是模拟面试主业务列表，承载发起模拟面试、查看进度、进入模拟面试页、进入复盘详情和查看详情。页面标题和 Penpot frame 必须使用“模拟面试列表”，不得继续使用“历史列表页”作为主页面名称。“历史记录”只允许作为模拟面试列表中的筛选、状态或说明。

必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| App Shell | 左侧导航选中“模拟面试 / 模拟面试列表”，右侧详情区显示模拟面试列表，右上角用户区保持可见。 |
| 页面主按钮 | 右侧详情区顶部提供主按钮“发起模拟面试”，进入 `/interviews/new`。 |
| 列表字段 | 模拟面试名称或编号、关联岗位、关联简历、模拟进度、模拟类型、面试主题、次主题、当前状态、最近更新时间、操作。 |
| 模拟进度 | 固定使用业务表达：未开始、进行中、待复盘、已完成、已中断。 |
| 模拟类型 | 固定包含：打磨模式、压力面模式。 |
| 操作 | 至少包含继续面试、查看复盘、重新发起、查看详情。 |
| 关联对象 | 每行必须展示关联岗位和关联简历；对象不可见时显示“部分资料不可见”，不泄露隐藏对象标识。 |
| 空态 | 暂无模拟面试，并引导用户点击“发起模拟面试”。 |
| 加载失败态 | 显示“模拟面试列表加载失败”，并提供“重新尝试”。 |

Codex 实现注意事项：

- `/interviews` 的 URL 后续可以保持不变，但页面标题、导航文案和 Penpot frame 名称必须使用“模拟面试列表”。
- 模拟面试列表不得展示 `unsafe_debug`、prompt、raw LLM response、object storage path 或 hidden resource id。
- “历史回看”是详情页或复盘页能力，不作为左侧主导航栏目名称。

### 5.3 发起模拟面试页

页面目标：`/interviews/new` 从模拟面试列表页的“发起模拟面试”按钮进入，帮助用户选择岗位、简历、模拟类型、主题、次主题和参考资料，然后进入模拟面试页。工作台首页可以提供快捷入口，但不能替代模拟面试列表页入口。

必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| App Shell | 左侧导航选中“模拟面试 / 发起模拟面试”，右侧详情区显示发起流程，右上角用户区保持可见。 |
| 岗位选择 | 显示最近岗位、岗位状态、必填标记、去新建岗位入口和缺失提示“请选择岗位”。 |
| 简历选择 | 显示最近简历、解析状态、必填标记、去上传 / 新建简历入口和缺失提示“请选择简历”。 |
| 模拟类型选择 | 必填，固定提供“打磨模式”和“压力面模式”，缺失时提示“请选择模拟类型”。 |
| 主题选择 | 必填，展示面试主题，缺失时提示“请选择主题”。 |
| 次主题选择 | 随主题变化展示次主题；未选主题时应有清晰占位。 |
| 参考资料选择 | 显示可用知识库、资料处理状态、是否启用参考资料、资料不可用或证据不足提示。 |
| 开始按钮 | 文案为“开始模拟面试”；在岗位、简历、模拟类型或主题缺失时 disabled，并说明缺失项。 |
| 开始后跳转 | 输入满足后进入模拟面试页。 |

主题和次主题示例：

| 主题 | 次主题 |
| --- | --- |
| 技术能力 | 前端工程、后端工程、系统设计、数据库、算法基础 |
| 项目经历 | 项目亮点、技术难点、协作推进、结果复盘 |
| 行为面试 | 沟通协作、冲突处理、自我驱动、压力应对 |
| 岗位匹配 | 岗位职责、必备技能、加分项、风险项 |

Codex 实现注意事项：

- 该页后续实现必须先有正式任务授权；本文档不授权新增面试创建 API。
- 低保真稿应预留“进入模拟面试页”的跳转关系，但不要假装当前代码已经具备完整模拟面试页。

### 5.4 模拟面试页

页面目标：承载多种模式的真实模拟面试窗口，是 R1 后续多轮面试能力的核心页面。该页用于系统提问、系统解读、用户回答、证据提示、回答评价摘要和后续语音 / 视频面试入口预留。

布局必须为：

| 区域 | 低保真要求 |
| --- | --- |
| 上方概括区 | 展示当前模拟名称或编号、关联岗位、关联简历、模拟类型、当前主题、当前次主题、当前轮次 / 当前问题序号、进行状态、资料不足或结果可信度降低提示。 |
| 左侧进展树 | 展示面试阶段、主题、次主题、问题节点、已回答 / 当前 / 待回答状态；允许查看已完成节点，并可标记需要复盘的问题。 |
| 右侧面试窗口 | 承载系统提问、系统解读、用户回答、参考资料提示、证据不足提示、当前回答评价摘要和可展开查看参考来源。 |
| 右下输入区 | 包含文本输入框、发送按钮、保存草稿或暂存、预留语音面试按钮、预留视频面试按钮。 |

进展树建议结构：

- 开场。
- 技术能力。
  - 前端工程。
  - 系统设计。
- 项目经历。
- 行为面试。
- 总结与复盘。

右侧面试窗口消息类型建议：

- 系统提问。
- 系统解读。
- 用户回答。
- 参考资料。
- 复盘提示。
- 风险提示。

输入区要求：

- 输入区必须明确当前回答将进入哪一题。
- 语音和视频按钮在 R1 可作为预留入口，不实现真实音视频。
- 页面文案应表达“语音面试”和“视频面试”，不得写技术术语。

Codex 实现注意事项：

- 本规格只定义低保真布局，不授权实现实时音视频、语音识别、视频流、房间服务或数据库变更。
- 模拟面试页与评分 / 复盘详情页职责不同：前者承载进行中的问答窗口，后者承载完成后的评分、复盘和证据链。

### 5.5 岗位管理页

页面目标：`/jobs` 展示岗位准备对象和投递状态，支撑从岗位查看详情、查看版本、查看关联简历和发起模拟面试。

岗位列表必须包含：

| 字段 | 低保真要求 |
| --- | --- |
| 岗位名称 | 可点击或通过操作进入岗位详情。 |
| 投递公司 | 每个岗位当前只关联一个投递公司。 |
| 投递进展 | 固定枚举：内推中、初筛中、一面、二面、三面。 |
| 关联简历 | 展示当前版本岗位关联的简历摘要；不可见时显示“部分资料不可见”。 |
| 版本 | 使用“版本”或“历史版本”，不得使用技术术语。 |
| 最近更新时间 | 显示业务可理解的更新时间。 |
| 操作 | 查看岗位详情、查看关联简历、查看历史版本、编辑岗位、发起模拟面试。 |

岗位详情页必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| 岗位基本信息 | 岗位名称、投递公司、投递进展、当前版本。 |
| 岗位要求 | 展示职责、必备技能、加分项、风险项等业务内容。 |
| 关联简历 | 支持查看不同版本岗位关联的简历。 |
| 关联模拟面试 | 展示与当前岗位相关的模拟面试入口。 |
| 历史版本入口 | 支持查看不同版本岗位详情。 |

Codex 实现注意事项：

- 当前 `/jobs` 仍可能是静态空态入口；Penpot 低保真稿应定义未来列表和详情形态，但不宣称后端已经完成。
- 岗位页不得成为复杂管理后台；R1 只服务模拟面试主链路。

### 5.6 简历管理页

页面目标：`/resumes` 展示简历材料准备对象，支撑从简历查看详情、查看历史版本、查看关联岗位、发起模拟面试和进行简历与岗位匹配度评估。

简历列表必须包含：

| 字段 | 低保真要求 |
| --- | --- |
| 简历名称 | 点击简历名称进入简历详情页。 |
| 关联岗位 | 展示简历关联岗位摘要。 |
| 当前版本 | 使用“当前版本”或“历史版本”，不得使用技术术语。 |
| 最近更新时间 | 显示业务可理解的更新时间。 |
| 解析状态 | 展示资料处理中、已完成、资料处理失败、部分资料不可用。 |
| 操作 | 查看简历详情、查看历史版本、查看关联岗位、发起模拟面试。 |

简历详情页必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| 简历基本信息 | 简历名称、当前版本、最近更新时间和解析状态。 |
| 历史版本入口 | 支持查看历史版本。 |
| 关联岗位 | 展示当前简历关联岗位，并可进入岗位详情。 |
| 简历内容预览 | 展示安全摘要，不展示对象存储路径。 |
| 简历和岗位匹配度评估 | 展示简历与岗位的匹配度评估，不等同于模拟面试评分。 |
| 多维度打分 | 建议维度：岗位匹配度、技能覆盖度、项目相关性、表达清晰度、风险与缺口。 |
| 简历评价 | 展示面向用户的评价，不展示内部模型提示词或原始响应。 |
| 薄弱点 | 展示薄弱点和风险项。 |
| 改进建议 | 展示简历改进建议，但不生成训练任务。 |

Codex 实现注意事项：

- 简历页不得展示原始对象存储路径。
- 简历与岗位匹配度评估独立于模拟面试评分；后续 E2E 需要分别覆盖。
- 如果后续上传能力未授权，前端只能展示入口和空态，不能伪造上传完成。

### 5.7 评分 / 复盘详情页

页面目标：`/interviews/:sessionId` 展示一场模拟面试的可信评分、复盘、证据链、关联岗位、关联简历和导出状态。

必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| App Shell | 左侧导航选中“复盘 / 评分 / 复盘详情”；如果从模拟面试列表进入，也保留合理高亮，右上角用户区保持可见。 |
| 关联对象 | 展示关联模拟面试、关联岗位、关联简历和模拟类型。 |
| 总分 | 显示综合评分、评分状态、低置信度提示；无分数时显示早期记录空态。 |
| 多维评分 | 每个维度显示维度名称、分数、评分理由、参考来源、证据不足、低置信度提示。 |
| 每个维度评分理由 | 评分理由必须和分数并列展示，不允许隐藏到 tooltip 中作为唯一入口。 |
| 参考来源 | 展示资料摘要、片段摘要、位置和引用标记。 |
| 证据不足 | 展示“暂无匹配资料”“部分资料不可见”“资料处理中”“资料处理失败”“参考资料暂不可用”等提示。 |
| 面试过程记录 | 展示面试场次、轮次、回答、评分、复盘、导出记录和过程信息数量。 |
| 复盘摘要 | 展示整体复盘摘要，不与维度评分理由混合。 |
| 改进建议 | 展示可执行建议列表。 |
| 薄弱项 | 展示薄弱项列表，并预留后续训练入口。 |
| 导出状态 | 展示导出状态、失败原因、是否可重新尝试和导出版本摘要。 |
| 敏感字段禁止展示 | 禁止展示 full prompt、raw LLM response、secret、object storage path、hidden resource id。 |

Codex 实现注意事项：

- 详情页应该采用“评分复盘摘要 -> 面试过程记录 / 参考资料 -> 导出记录 / 请求引用摘要”的阅读顺序，具体列数和区域位置以 Penpot frame 为准。
- request refs 只能展示安全 label，不展示 provider 原始请求体。
- 证据不足和结果可信度降低不是错误噪声，应作为可信度信息长期可见。

### 5.8 复盘列表页

页面目标：`/reviews` 汇总最近复盘，帮助用户从评分结果回到可信详情，并能关联模拟面试、岗位和简历。

复盘列表字段建议：

| 字段 | 低保真要求 |
| --- | --- |
| 复盘名称或面试编号 | 可进入复盘详情。 |
| 关联岗位 | 显示岗位摘要，并可查看关联岗位。 |
| 关联简历 | 显示简历摘要，并可查看关联简历。 |
| 综合评分 | 显示“综合评分：82”或“暂无评分”。 |
| 模拟类型 | 展示打磨模式或压力面模式。 |
| 主题 | 展示主题，可附带次主题。 |
| 最近更新时间 | 显示业务可理解的更新时间。 |
| 状态 | 展示复盘、过程记录、导出和可重新尝试状态。 |
| 操作 | 查看复盘详情、查看关联模拟面试、查看关联岗位、查看关联简历。 |

Codex 实现注意事项：

- `/reviews` 可以复用已有记录 contract，但 UI 语义必须是“复盘列表”，不能只是模拟面试列表换标题。
- 后续如果新增独立 review API，需要同步更新 E2E，不得破坏 `/interviews/:sessionId` 详情入口。

### 5.9 知识库

页面目标：为参考资料、文档资料和资料处理状态预留明确入口，并和发起模拟面试、评分复盘证据链关联。

必须包含：

| 区块 | 低保真要求 |
| --- | --- |
| App Shell | 左侧导航选中“知识库”，右侧详情区显示资料和资料处理状态，右上角用户区保持可见。 |
| 我的资料 | 展示用户私有资料列表、可见性、上传 / 解析状态和最近使用。 |
| 公共知识库 | 展示公共资料入口和权限说明，不展示不可见资源名称。 |
| 资料处理状态 | 展示已处理、资料处理中、资料处理失败、部分资料不可用、参考资料暂不可用等状态。 |
| 可用于哪些模拟面试主题 | 展示资料可用于技术能力、项目经历、行为面试、岗位匹配等主题的提示。 |
| 资料不可用提示 | 显示资料不可用时对发起模拟面试和复盘可信度的影响。 |
| 资料处理失败提示 | 显示失败原因和可重新尝试动作。 |
| 证据不足提示 | 展示复盘中可能出现证据不足的原因。 |
| 空态 | 没有资料时提示可先继续发起模拟，但复盘会标注证据不足。 |

Codex 实现注意事项：

- 本文档只预留知识库入口，不授权新增知识库 route、上传 API、索引后台或数据库 schema。
- 知识库栏目必须出现在左侧主导航，但旧 mock 不得作为同级栏目出现。

## 6. Penpot frame 规划

所有 Penpot frame 必须带统一 App Shell：左侧全局导航、右侧页面详情区、右上角用户登录信息区。每个 frame 都必须标注左侧导航选中态、右上角用户区、页面标题、主内容区域、状态区、主要操作、空态 / 失败态 / 降级态。

Penpot frame 文字规则：

1. frame 画布正文只使用用户可见业务语言。
2. 技术术语只能写在 frame 注释、本规格的实现说明或验收规则中。
3. 左侧导航必须使用业务栏目名：工作台总览、简历和岗位管理、模拟面试、复盘、知识库。
4. 右上角用户区使用：用户头像、用户名称、个人配置、偏好设置、退出登录。
5. 状态区使用业务文案，不出现英文技术状态。

本轮更新后的 Penpot frame 固定为：

| Frame | 页面标题 | 左侧导航选中态 | 主内容区域 | 主要操作 / 状态 |
| --- | --- | --- | --- | --- |
| `R1 Workbench Home` | `AI 模拟面试工作台` | 工作台总览 | 工作台概览、主操作区、最近模拟面试、复盘能力摘要、岗位 / 简历 / 知识库概览。 | 发起模拟面试、进入模拟面试列表、进入岗位、进入简历、进入复盘；暂无记录、证据不足、导出失败时可重新尝试。 |
| `R1 Jobs List` | `岗位管理` | 简历和岗位管理 / 岗位管理 | 岗位名称、投递公司、投递进展、关联简历、版本、最近更新时间、操作。 | 查看岗位详情、查看关联简历、查看历史版本、编辑岗位、发起模拟面试；暂无岗位、部分资料不可见。 |
| `R1 Job Detail and Versions` | `岗位详情` | 简历和岗位管理 / 岗位管理 | 岗位基本信息、投递公司、投递进展、当前版本、岗位要求、关联简历、关联模拟面试、历史版本入口。 | 查看不同版本岗位、查看版本关联简历、发起模拟面试；版本不可用、部分资料不可见。 |
| `R1 Resumes List` | `简历管理` | 简历和岗位管理 / 简历管理 | 简历名称、关联岗位、当前版本、最近更新时间、解析状态、操作。 | 点击简历名称进入详情、查看历史版本、查看关联岗位、发起模拟面试；暂无简历、资料处理失败。 |
| `R1 Resume Detail and Match Evaluation` | `简历详情` | 简历和岗位管理 / 简历管理 | 简历基本信息、当前版本、历史版本入口、关联岗位、简历内容预览、简历和岗位匹配度评估、多维度打分、评价、薄弱点、改进建议。 | 查看历史版本、查看关联岗位、发起模拟面试；资料不足、评估暂不可用。 |
| `R1 Simulation Interview List` | `模拟面试列表` | 模拟面试 / 模拟面试列表 | 模拟面试名称或编号、关联岗位、关联简历、模拟进度、模拟类型、面试主题、次主题、当前状态、最近更新时间、操作。 | 发起模拟面试、继续面试、查看复盘、重新发起、查看详情；暂无模拟面试、模拟面试列表加载失败。 |
| `R1 Start Simulation Interview` | `发起模拟面试` | 模拟面试 / 发起模拟面试 | 选择岗位、选择简历、选择模拟类型、选择面试主题、选择次主题、是否启用参考资料、资料不可用或证据不足提示。 | 开始模拟面试；请选择岗位、请选择简历、请选择模拟类型、请选择主题。 |
| `R1 Simulation Interview Room` | `模拟面试` | 模拟面试 | 上方当前面试概括区、左侧进展树、右侧面试窗口、右下输入区。 | 发送、保存草稿或暂存、语音面试、视频面试、标记需要复盘；资料不足、结果可信度降低。 |
| `R1 Reviews List` | `复盘列表` | 复盘 / 复盘列表 | 复盘名称或面试编号、关联岗位、关联简历、综合评分、模拟类型、主题、最近更新时间、状态、操作。 | 查看复盘详情、查看关联模拟面试、查看关联岗位、查看关联简历；暂无复盘、生成失败、证据不足。 |
| `R1 Trusted Review Detail` | `评分 / 复盘详情` | 复盘 / 评分 / 复盘详情 | 总分摘要、多维评分、复盘摘要、改进建议、薄弱项、面试过程记录、参考来源、证据不足、导出状态、请求引用摘要。 | 查看参考来源、查看证据不足、复制 / 下载导出入口占位、返回模拟面试列表；早期记录暂无完整过程信息、暂无评分复盘。 |
| `R1 Knowledge Base` | `知识库` | 知识库 | 我的资料、公共知识库、资料处理状态、可用于哪些模拟面试主题、资料不可用提示、资料处理失败提示、证据不足提示。 | 查看资料、补充资料、查看资料处理状态；没有资料、资料处理失败、参考资料暂不可用。 |
| `R1 Empty and Degraded States` | `空态和异常状态` | 按来源高亮对应栏目 | 暂无模拟面试、暂无岗位、暂无简历、暂无复盘、资料处理中、资料处理失败、部分资料不可用、证据不足、部分资料不可见、早期记录暂无完整过程信息、早期记录暂无评分。 | 发起模拟面试、新建岗位、上传 / 新建简历、返回工作台、重新尝试、继续并提示结果可信度降低。 |

## 7. Penpot MCP 后续工作流

后续使用 Penpot MCP 时必须按以下流程执行：

1. 用户先在 Penpot 中打开目标设计文件，并通过 Penpot MCP Plugin 连接当前项目。
2. Codex 使用 Penpot MCP 读取当前 focused page / current page，确认 page 名称、已有 frame 和 selection。
3. Codex 根据本文档整理将创建的 frame、统一 App Shell、左侧导航选中态、右上角用户区、尺寸、区块、组件层级、命名规则和状态覆盖计划。
4. 创建任何 Penpot frame 前，Codex 必须先输出计划并等待用户确认。
5. 用户确认后，Codex 通过 Penpot MCP 创建低保真 frame；创建时优先使用 board、text、rectangle、group、flex / grid layout 等基础形状，不做高保真视觉。
6. Codex 创建后应读取 frame structure，并在必要时导出 frame 预览用于检查。
7. 用户在 Penpot 中手工调整 frame，包括布局、区块顺序、信息密度和文案。
8. 用户明确确认 frame 后，Codex 再通过 Penpot MCP 读取设计稿结构、导出预览，并把 Penpot frame 作为最高优先级布局依据。
9. Codex 按用户确认的 Penpot frame 修复前端页面。
10. Playwright 增加 screenshot baseline，覆盖首页、模拟面试列表、发起模拟面试、模拟面试页、复盘详情、空态、业务异常状态和旧 mock 隔离。
11. E2E 必须继续覆盖根路径、模拟面试列表、发起模拟面试、模拟面试页、岗位列表字段、简历详情匹配度、复盘与模拟面试关联、空态、部分资料不可用 / 加载失败 / 可重新尝试、敏感字段不展示和旧 mock 隔离。
12. 未经用户确认，Codex 不允许自行改变 Penpot 信息架构、删除用户手工调整、合并 frame 或新增不在本文档范围内的页面。

Penpot MCP 执行注意事项：

- 读取结构时优先使用 `penpotUtils.getPages()`、`penpot.root`、`penpot.selection` 和 `penpotUtils.shapeStructure()`。
- 创建 frame 时按视觉顺序 append child；如果 board 使用 flex / grid layout，优先修改 layout gap / padding，不手动硬改子元素坐标。
- 创建文字元素时避免重复写 shape 名称；Penpot 图层名和画布文字职责应分开。
- 输出给用户的计划必须列出 frame 名称、左侧导航选中态、右上角用户区、页面区块、主要状态和不会修改代码的保证。
- 后续从 Penpot 到代码时，缺失的样式值不能由 Codex 自行发挥，应先标注缺失并向用户确认。

## 8. 后续页面实现验收原则

后续页面实现必须遵守：

- Penpot frame 是最高优先级布局依据。
- 现有文字描述只作为辅助。
- 不能只凭现有代码推断布局。
- 根路径 `/` 必须显示统一 App Shell，并符合 Penpot 首页。
- 左侧全局导航在主要页面保持一致。
- 右上角用户头像、用户名称、个人配置、偏好设置和退出登录入口在主要页面保持一致。
- “历史列表页”不再作为主页面名称，应使用“模拟面试列表页”。
- `/jobs`、`/resumes` 或合并后的“简历和岗位管理”入口必须来自左侧导航。
- 岗位列表必须展示投递公司、投递进展、关联简历、版本。
- 岗位详情必须支持版本与关联简历查看。
- 简历列表必须展示关联岗位、版本，并可点击简历名称进入简历详情。
- 简历详情必须包含简历与岗位匹配度评估。
- `/interviews`、`/interviews/new` 必须来自“模拟面试”栏目。
- `/interviews` 必须符合 Penpot 模拟面试列表。
- 模拟面试列表必须展示关联岗位、关联简历、模拟进度、模拟类型。
- 发起模拟面试入口必须来自模拟面试列表页按钮。
- 发起模拟面试必须支持打磨模式和压力面模式。
- 发起模拟面试必须支持主题和次主题选择。
- 模拟面试页必须有左侧进展树、右侧面试窗口、右下输入区、上方面试概括。
- `/interviews/:sessionId` 必须符合 Penpot 评分复盘详情。
- `/reviews` 必须来自“复盘”栏目。
- 复盘必须能关联模拟面试、岗位和简历。
- 知识库栏目必须为参考资料、文档资料和资料处理状态保留入口。
- 旧 mock 只能在 `/legacy-mock` 或 `/mock`。
- 旧 mock 不得作为左侧主导航项。
- 不展示 full prompt、raw LLM response、secret、object storage path、hidden resource id。
- 页面主文案不出现数据库、测试、内部 gate、traceability、RAG citation、degraded、failed、empty 等工程术语。
- 用户能理解每个状态的业务含义，状态文案应使用“暂无记录”“资料处理中”“资料处理失败”“参考资料暂不可用”“部分资料不可见”“可重新导出”等表达。
- 工程术语只保留在实现注释、验收说明或开发文档中，不进入页面主文案。
- 旧 mock 文案只作为禁止项，不作为页面内容。
- 根路径不得出现旧 mock 主界面文案。
- 旧 mock 只能保留在 `/legacy-mock` 或 `/mock`。
- Penpot 主工作台 frame 不得包含旧 mock 原型文案。
- 页面能力必须有 Playwright E2E。
- Playwright E2E 后续应断言首页不出现旧 mock 文案。
- Playwright E2E 后续应断言页面出现业务状态文案，例如“暂无记录”“资料处理中”“可重新导出”。
- Playwright E2E 后续必须覆盖模拟面试列表、发起模拟面试、模拟面试页、岗位列表字段、简历详情匹配度、复盘与模拟面试关联。
- Playwright E2E 后续必须覆盖左侧导航可见、用户头像可见、用户下拉可打开、左侧导航进入各主要栏目、详情区内容随路由变化。
- 关键页面必须有 screenshot baseline。
- R1 未被用户确认 closeout 前，不得把本设计规格解释为 R1 acceptance closeout 已完成。

## 9. 后续窗口规划

| Window ID | 目标 | 修改范围 |
| --- | --- | --- |
| `R1-UX-02-PENPOT-LOWFI-GENERATION` | Codex 使用 Penpot MCP 生成本文档定义的低保真 frame。 | 只改 Penpot，不改代码。 |
| `USER-PENPOT-REVIEW` | 用户在 Penpot 中手工调整 frame，并确认最终低保真稿。 | Penpot 手工调整。 |
| `R1-FE-03-PENPOT-DESIGN-IMPLEMENTATION` | Codex 读取用户确认的 Penpot frame 并修复前端页面。 | 以后续窗口卡授权为准。 |
| `R1-FE-04-VISUAL-E2E-CLOSEOUT` | 增加 Playwright screenshot baseline 和 E2E 收口。 | 以后续窗口卡授权为准。 |
| `R1-CLOSEOUT-02` | 在视觉和 E2E 收口后执行 R1 acceptance closeout。 | 以后续 closeout 窗口授权为准。 |

## 10. 下一窗口卡：`R1-UX-02-PENPOT-LOWFI-GENERATION`

| 字段 | 内容 |
| --- | --- |
| Window ID | `R1-UX-02-PENPOT-LOWFI-GENERATION` |
| Iteration | R1 |
| Task Mode | UX specification / Penpot generation / no code |
| Goal | 使用 Penpot MCP 在当前 focused page 创建带统一 App Shell 的 R1 低保真 frame。 |
| Existing Source | `docs/design/workbench-mvp/r1-penpot-lowfi-spec.md` |
| Allowed Paths | Penpot 当前连接文件；本地仓库无写入。 |
| Forbidden Paths | `apps/**`、`tests/**`、`docs/governance/**`、`DOC_STATE.yaml`、packet、数据库、`.env`、package 依赖。 |
| Input Docs | `docs/design/workbench-mvp/r1-penpot-lowfi-spec.md`、`docs/development/r1-trusted-trace-ui-compliance.md`、当前 Penpot focused page。 |
| DoD | 12 个指定 frame 已创建或已明确说明无法创建的阻断：`R1 Workbench Home`、`R1 Jobs List`、`R1 Job Detail and Versions`、`R1 Resumes List`、`R1 Resume Detail and Match Evaluation`、`R1 Simulation Interview List`、`R1 Start Simulation Interview`、`R1 Simulation Interview Room`、`R1 Reviews List`、`R1 Trusted Review Detail`、`R1 Knowledge Base`、`R1 Empty and Degraded States`；frame 名称、左侧导航选中态、右上角用户区、区块、关键文案、状态集合与本文档一致。 |
| Validation | Penpot MCP 读取当前 page structure；必要时导出 frame 预览；本地执行 `git status --short` 确认仓库未变。 |
| Change Budget | 本地文件 0；Penpot frame 12 个。 |
| Stop Conditions | Penpot MCP 未连接；focused page 不明确；用户未确认创建计划；现有 Penpot 信息架构与本文档冲突且用户未决策。 |
| Writeback Target | none |

目标偏离：否。当前动作服务于 R1 的页面实现前设计校准，不进入业务代码实现或 R1 closeout。

推进缓慢风险：否。本文档解除“后续页面只能靠文字和现有代码推断布局”的设计输入阻断，下一步直接进入 Penpot 低保真生成窗口。
